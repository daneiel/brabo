---
id: adr-index
title: Decisões arquiteturais (ADR)
sidebar_label: Índice de ADRs
sidebar_position: 0
description: Os 68 registros de decisão arquitetural do Brabo, agrupados por fase, com o que cada um decidiu.
keywords: [ADR, decisão arquitetural, arquitetura, histórico]
---

# Decisões arquiteturais

Um ADR registra **por que** algo foi decidido, no momento em que foi decidido.
Eles são **imutáveis**: nenhum ADR é editado depois de aceito. Quando uma
decisão muda, nasce um ADR novo que substitui o anterior — foi o que aconteceu
com o [0018](0018-noop-dev-agent-modo-de-execucao.md), que registra a
substituição do NoopDevAgent decidida no [0011](0011-infra-dev-agents-worktrees-merge-lock.md).

Consequência prática: **um ADR pode descrever um estado que não é mais o
atual.** Para o estado atual, use [Regras de negócio](../business-rules.md),
[Arquitetura](../architecture.md) e o [Runbook](../runbook.md). Para o
raciocínio, venha aqui.

Dos 61, todos estão **aceitos** menos os que ainda esperam implementação — hoje o [0053](0053-dev-lead-e-paralelismo-autorizado.md), o [0054](0054-gates-como-registro-declarativo.md) e o [0056](0056-o-engine-trabalha-em-repositorio-remoto.md), marcados **propostos** no próprio arquivo. O [0055](0055-escopo-de-caminho-na-politica-de-terminal.md) foi aceito ao ser implementado na Fase F. Nenhum check confere esta frase: ela envelheceu dizendo 48 quando já eram 54, e de novo 58 quando já eram 59, e é o argumento do ADR 0029 (`gerar > verificar > lembrar`) se confirmando contra si mesmo.

## Fase 2 — Git

O contrato que faz Local, GitHub e GitLab serem intercambiáveis.

| # | decisão |
|---|---|
| [0001](0001-git-provider-contract-shape.md) | Formato do contrato normalizado do GitProvider: nove operações e `capabilities` declaradas, validadas por uma suite de contrato única |
| [0002](0002-git-error-normalization.md) | Normalização de erros: seis classes específicas, **sem** classe-base comum — a abstração foi considerada e rejeitada por não pagar o próprio custo ainda |
| [0003](0003-git-provider-retry-policy.md) | Retry automático **só em leituras, nunca em mutações**, com Full Jitter e 4 tentativas |
| [0004](0004-git-credential-registration.md) | Cadastro de credenciais de git: enum dedicado no banco, união com os providers de LLM apenas no tipo |
| [0005](0005-repo-bootstrap-idempotent-steps.md) | Bootstrap de Gitflow em seis passos idempotentes e retomáveis; `skip` é sucesso |

## Fase 3 — Harness e primeiros agentes

O invólucro que torna o comportamento do agente reproduzível.

| # | decisão |
|---|---|
| [0006](0006-prompt-assembler-context-harness.md) | Montagem **determinística** de contexto: camadas ordenadas, orçamento por camada, corte previsível |
| [0007](0007-toolloop-tools-context-manager.md) | ToolLoop, catálogo de ferramentas e ContextManager — o laço com LLM de verdade |
| [0008](0008-agente-criativo-conversacional-handoffs.md) | Criativo conversacional (o chat deixa de ser echo stateless) e a fundação de handoffs |
| [0009](0009-agente-po-backlog-rastreabilidade.md) | PO, backlog em três níveis e rastreabilidade regra → história → task; a prontidão vira validação de domínio |
| [0010](0010-agente-arquiteto-adr-pr-module-map.md) | Arquiteto: ADRs por PR real, `module_map` e validação cruzada |

## Fase 4a — Execução e gates

Onde o sistema passa a mexer em código de verdade — e onde nascem os limites.

| # | decisão |
|---|---|
| [0011](0011-infra-dev-agents-worktrees-merge-lock.md) | Worktrees por dev agent, executores git e a **trava de merge**: branch protegida nunca é auto-aprovável |
| [0012](0012-dev-agent-real-toolloop-orcamento-bloqueio.md) | DevAgent real: ToolLoop no worktree, orçamento por task, `ReportDone` exigindo suite verde, bloqueio com diagnóstico |
| [0013](0013-gates-qa-secops-pr.md) | Gates de QA e SecOps com máquina de estados de ordem imutável |
| [0014](0014-infra-agent-painel-phoenix.md) | InfraAgent **propositivo** (propõe por PR, nunca aplica) e painel do time ao vivo por canais Phoenix |
| [0017](0017-lock-de-workspace-e-monitor-de-dev-agents.md) | Lock de inicialização do workspace, Monitor de dev agents e **herança do teto** de correções pelo subagente |
| [0018](0018-noop-dev-agent-modo-de-execucao.md) | O NoopDevAgent deixa de ser andaime e vira **modo de execução permanente** |
| [0019](0019-destravar-dev-agent-adr-por-modulo-orcamento-projeto.md) | Destrava do DevAgent real: escrita, terminal, ADR por módulo e orçamento por projeto |
| [0020](0020-destravar-gates-qa-secops.md) | Destrava dos gates: gitleaks na **árvore de trabalho** (não só no diff), pareceres validados como artefato, recuperação de tool call em texto |
| [0021](0021-fechamento-4a-infra-e-painel.md) | Fechamento da 4a: gate de infra que valida de verdade, e painel que diz a verdade |

> **Critério de aceite em aberto.** O [0020](0020-destravar-gates-qa-secops.md)
> registra explicitamente que o demo de gates é **"FECHADO, mas NÃO
> DETERMINÍSTICO"** — depende do julgamento de um modelo 7B local e não serve
> como teste de regressão. O [0021](0021-fechamento-4a-infra-e-painel.md) tem
> uma seção "Estado do critério de aceite: NÃO FECHADO". Os dois são honestos
> de propósito; não os leia como concluídos.

## Fase 4b — Psicólogo e Anamnese

O loop que faz o time melhorar.

| # | decisão |
|---|---|
| [0015](0015-psicologo-real-toolloop-hipoteses-evidencia.md) | Psicólogo real: hipóteses com **evidência apontando para eventos reais**, e triagem leve/pesada por custo |
| [0016](0016-anamnese-proficiencia-patches-instrucao.md) | Anamnese: catálogo **fechado** de competências, patches de instrução versionados com rollback, e o teto de que patch nunca é auto-aprovável |
| [0022](0022-fechamento-4b-psicologo.md) | Evidência que chega, custo que aparece, análise que sobrevive ao kill; causa de término como classificação determinística |
| [0023](0023-fechamento-4b-anamnese.md) | Catálogo que funciona, dedup que distingue **quem** negou, e fila que sobrevive à rodada |

## Fase 5 — Produção

| # | decisão |
|---|---|
| [0024](0024-fase5-imagens-producao-ci.md) | Imagens de produção multi-stage e non-root, `compose.prod`, CI com scan e teste de fumaça |
| [0025](0025-fase5-deploy-kubernetes-kustomize.md) | **Kustomize** base+overlays (não Helm), HPA do engine por profundidade da fila do Oban, overlay local |
| [0026](0026-fase5-observabilidade-e-graceful-shutdown.md) | Graceful shutdown com drain, registro de sessão em `:global`, OpenTelemetry com trace por sessão, métricas e dashboards como código |
| [0027](0027-fase5-backup-hardening-release.md) | Backup/restore testado, rate limit em Postgres (sem Redis), superfície HTTP classificada e release versionado |
| [0028](0028-protecao-de-branch-divergencia-entre-providers.md) | Proteção de branch diverge entre GitHub e GitLab — e por isso **não é o portão**; o portão é o domínio |

## Documentação

| # | decisão |
|---|---|
| [0029](0029-sincronizacao-continua-da-documentacao.md) | Sincronização contínua da documentação: **gerar > verificar > lembrar**, mapa de responsabilidade validado no CI, e escape hatch obrigatório |

## Política de branches

| # | decisão |
|---|---|
| [0030](0030-politica-de-branches-mecanizada.md) | Política de branches mecanizada: regra → mecanismo, lógica em script testável, gate de retropropagação, e o custo de reintroduzir cada corte |

## Fase 7 — Auth first-party

| # | decisão |
|---|---|
| [0031](0031-auth-first-party-argon2id-e-rotacao-de-refresh.md) | Auth no domínio da api: argon2id com parâmetros fixos, access token EdDSA de chave derivada, rotação de refresh com revogação de família por reuso, lockout com chave no e-mail (e não no id) e a invariante única que fecha a enumeração |

## Fase 7 — O corte

| # | decisão |
|---|---|
| [0032](0032-corte-do-keycloak-e-sessao-em-cookie.md) | O corte atômico do Keycloak: emissor próprio no guard sem tocar no RBAC, `/internal/*` fora do JWT com segredo compartilhado, sessão da web em cookie httpOnly com CSRF, e o usuário migrado indistinguível de um e-mail que não existe |
| [0033](0033-referencia-de-api-gerada-do-openapi.md) | A referência de API sai do código: DTOs de resposta que espelham a entidade por tipo, o teste de tabela cobrando summary e resposta de verdade (a sintetizada pelo Swagger não conta), e um manifesto de hashes no lugar de regerar no `--check` |
| [0034](0034-documentacao-publicada-por-degrau.md) | Documentação publicada por degrau em subdiretório do Pages: `baseUrl` por variável, `noIndex` fora da `main` com `forceIgnoreNoIndex` na busca local, e a terceira exceção de push direto |
| [0035](0035-observabilidade-legivel-e-trace-sem-coletor.md) | Instrumentar e exportar separados (trace com `trace_id` real sem coletor), o caminho entre camadas por `AsyncLocalStorage` + `@Traced`, log legível em dev e uma linha de JSON em produção, e os três furos de correlação que estavam mortos |
| [0036](0036-telas-de-auth-fieis-ao-design-e-fontes-auto-hospedadas.md) | As telas de auth fiéis ao mock aprovado: as três fontes do design system auto-hospedadas (a CSP bloqueava o CDN e a tipografia caía em fonte de sistema em produção), a versão da tag assada no artefato, o contraste calculado dos tokens em vez de medido pelo axe, e as seis divergências deliberadas do mock |
| [0037](0037-cors-do-engine-e-a-porta-como-contrato.md) | O CORS que o engine não tinha (`/health` respondia 200 e o navegador descartava), o plug próprio em vez do Corsica com `/internal` e `/metrics` fora da allowlist, `WEB_ORIGIN` lido num lugar só, e `strictPort` porque a porta faz parte do contrato de CORS |

## Fase 8 — Hierarquia de agentes

| # | decisão |
|---|---|
| [0038](0038-hierarquia-de-agentes.md) | Área, lead e delegação: handoff externo só endereça lead ou agente sem área, consolidação num artefato só sem mudar o contrato externo dos gates, orçamento em cascata com sub-teto por delegação, origem da falha tipada e retrofitada em todo o sistema que já classificava falha informalmente |
| [0039](0039-actionlint-e-validacao-do-pipeline-de-ci-gerado.md) | actionlint pinado no Dockerfile do engine (mesmo padrão do gitleaks/hadolint), validação do pipeline de CI na GERAÇÃO em vez de num gate pós-PR novo, e o gap documentado de `.gitlab-ci.yml` sem linter estático offline equivalente |

## Painel de projetos

| # | decisão |
|---|---|
| [0040](0040-moeda-do-dashboard.md) | O card do dashboard e a linha de resumo mostram só USD (a moeda de origem dos preços, sem conversão nenhuma envolvida) — divergência isolada a essa superfície, `TokenMeter` `default`/`live` continuam R$+US$; preferência de moeda por workspace com taxa manual fica registrada e adiada |

## Fase 9 — Providers de LLM

| # | decisão |
|---|---|
| [0041](0041-base-openai-compativel-e-contrato-de-llm-providers.md) | Base OpenAI-compatível sobre `node:http` (não sobre o SDK, para ter timeout de INATIVIDADE), erro normalizado com `code` obrigatório no chunk, capabilities em duas camadas com binding de agente recusado sem tool calling nativo, e suite de contrato onde o contrato é dono das asserções e o harness do dialeto |
| [0042](0042-catalogo-vivo-ciclo-de-vida-do-modelo-e-preco-auditavel.md) | Sync de catálogo por capability, com modelo descoberto entrando desativado e modelo sumido marcado em vez de apagado; dois eixos independentes de disponibilidade (curadoria do owner × realidade remota); cascata que revalida capability ao cair de nível; e preço congelado em `token_usage` para o custo antigo ficar reproduzível, com auditoria append-only fora do outbox |
| [0043](0043-seis-providers-de-llm-e-o-fechamento-da-fase-9b.md) | Os seis providers da 9b (NVIDIA NIM, Together, DeepInfra, Bitdeer, Vultr, OpenRouter) entram como config sobre a base, cada um investigado do zero contra a doc oficial — capability só declarada quando provada, com dois casos reais de decisão revertida ao vivo durante a implementação; único hook novo na base é o `parseErrorFrame` que só o OpenRouter (o hub) precisou |

## Fase 12 — Operabilidade pós-dogfooding

| # | decisão |
|---|---|
| [0044](0044-adocao-de-repositorio-existente.md) | Adoção de repositório existente com `origin` (`created`\|`adopted`) nas duas tabelas, e o PLANO como portão: o dry-run é o `check()` do ADR 0005 sem `run()`, o plano vive no cursor (snapshot, não log), e nada é alterado enquanto a decisão for nula — o runner da Fase 2 sai verbatim para um colaborador e nunca é filtrado, só não é chamado; "proteção divergente" fica no nível booleano porque é só isso que o contrato expõe (ADR 0028) |
| [0045](0045-reagendamento-por-evento-do-dev-agent.md) | Reagendamento do dev agent por evento: outbox (não chamada em processo) porque sobrevive a um restart entre o veredito do gate e o wake; `awaiting_gate` retém o worktree por AGENTE (não por task) até o gate terminar; entrega por PubSub (não Registry, local ao nó) com o limite at-most-once aceito conscientemente; circuit breaker (RN-047) mora no engine porque `actor.id` de `backlog.task_blocked` não identifica a sequência de um agente |
| [0046](0046-promocao-de-story-com-autoridade-do-usuario.md) | Promoção de história volta a ser do usuário: `proposed_ready` é booleano e não valor novo no enum porque o enum É o portão do `claimNext`; promover reusa o `TransitionStoryUseCase` (código morto do achado #13) e herda de graça o `task.became_claimable` da 12b; a validação é unificada em `assertPromotable` ANTES de o gatilho virar configurável, senão os dois modos seriam regras distintas com o mesmo nome; backfill DIRIGIDO (projeto existente vai para `auto`) porque o comportamento estava em uso; a recusa grava antes de falar com o engine — inverso do rearm, porque o evento afirma algo sobre o USUÁRIO, não sobre o engine |
| [0047](0047-operabilidade-pos-dogfooding.md) | Fechamento da Fase 12: a validação é um SCRIPT que sai != 0, não um roteiro em prosa, e a evidência é extraída do banco em vez de transcrita; roda local e sem LLM por dois motivos verificados (o fork nunca foi nomeado; o julgamento dos gates com modelo local não é determinístico desde o ADR 0020), e isso é declarado no primeiro parágrafo e não no rodapé; o Noop entrou na máquina de estados da 12b — o achado #10 sobrevivia dentro do próprio instrumento de medida —, movendo-a para o `AgentIo` em vez de copiá-la; a colheita da Fase 10 foi escrita com os buracos como `não medido` |
| [0048](0048-decisao-no-log-e-a-ordem-do-gate.md) | Dois achados revisitados: a DECISÃO de uma ação (`created`/`approved`/`denied`) vira evento de domínio com o ator real — o outbox é transporte, não memória, e `events.md` documentava os três desde sempre sem que existissem; `status` no payload do `created` é o que distingue clique humano de auto-aprovação por política, a métrica que a Fase 10 não conseguiu colher. E o D5: a causa não era o worktree ser reciclado, era `propose/3` descartar o status e o gate abrir sem PR — o QA varria o worktree, aprovava, e a task fechava sem uma linha commitada. Corrigida a ordem, o D5 morre por consequência, e worktree-por-task (previsto pelo ADR 0045) deixa de ser necessário |

| [0049](0049-curadoria-de-modelo-por-workspace.md) | Curadoria de modelo por workspace: `models.is_active` era uma coluna para a instalação INTEIRA — um owner do workspace A ligando um modelo o ligava para o B, com o gasto caindo no orçamento de quem não decidiu. O catálogo segue global porque nome, preço e capabilities são fato do provider, e duplicá-lo por workspace criaria N verdades sobre o mesmo modelo além de partir `token_usage.model_id`; só a decisão migra, para `workspace_models`. Ausência de linha É o desligado — não há terceiro estado, e é assim que a RN-043 sobrevive sem coluna nenhuma que o sync possa atropelar; desligar é UPDATE e não DELETE, para não apagar quem decidiu junto. A rota do seletor pende do PROJETO (as três telas que a consomem já tinham `projectId`, nenhuma tinha workspace), e `isActive` sai da entidade `Model`: quem precisa dele usa um tipo que só existe com workspace na mão |

| [0050](0050-credencial-sempre-cifrada-verificacao-explicita.md) | Credencial sempre cifrada e gravada; verificar vira ação à parte. O portão do ADR 0004 (testar ANTES de persistir) parecia prudente e produzia o pior desfecho: campo write-only + recusa = usuário sem credencial E sem o texto para corrigir, porque a tela nunca reexibe o que ele colou. Guardar e verificar são perguntas diferentes, e amarrá-las fazia a segunda depender da primeira ter resposta agora. Vale para LLM e git juntos — mesma tabela, mesma promessa, e tratá-las diferente daria duas telas com regras distintas para o mesmo objeto. O veredito tem TRÊS estados porque o tester é NO-OP em ollama/anthropic/openai: num binário eles voltariam `ok` e a tela afirmaria uma verificação que ninguém fez (a regra de capability do ADR 0041 aplicada aqui). Chave ruim deixa de ser exceção HTTP e vira resultado 200, então os dois `@Catch` que a mapeavam saíram — filtro que não pode disparar é regra morta |
| [0051](0051-facetas-de-capability-e-curadoria-por-uso.md) | Facetas de capability lidas do provider, e "para que serve" como curadoria do workspace. O sync nunca consultava o remoto para modalidade (`supportsVision: local?.supportsVision ?? false`), então a coluna nascia `false` e morria `false` — os 338 modelos do primeiro sync real do OpenRouter ficaram todos sem vision, incluindo os 181 que o provider declara multimodais. Entrada e saída de imagem viram eixos DISTINTOS: quem lê diagrama e quem desenha resolvem problemas diferentes. A outra metade do pedido — "melhor para código/documentação" — não existe em catálogo nenhum, e derivá-la do nome do modelo seria palpite vestido de dado (ADR 0041); então ela é opinião de quem opera e mora em `workspace_models`, como toda curadoria desde o ADR 0049. Os dois eixos não se misturam: marcar uso não liga o modelo (a coluna `is_active` tem DEFAULT `true`, e a linha nova nasce com `false` explícito), e a tela nunca escreve "não lê imagem", porque `false` ali quer dizer "o provider não declarou". Vídeo ficou de fora: zero modelos em nove providers, um filtro que nunca casaria nada |
| [0052](0052-dev-agent-espera-aprovacao-no-meio-do-laco.md) | O dev agent espera a aprovação no meio do laço, em vez de queimar iterações. Ferramenta `:pipeline` pendente devolvia a string `proposed_action <id> status pending` como RESULTADO, e o ToolLoop seguia: o agente não aprendia nada sobre o comando, tentava outra coisa, e morria em `toolloop.limit_reached {iteration: 8, max_iterations: 8}` sem escrever uma linha — as aprovações concedidas chegavam depois do laço esgotado e eram inúteis. A allowlist de terminal não resolve porque é lista de comandos previstos e o modelo inventa comandos: liberados os de leitura, o mesmo agente travou em `git branch -a`. `:awaiting_approval` JÁ existia (Fase 12e, para o `pr_open` do fim da task) com todo o encanamento pronto — evento de outbox, worker, `Wake.deliver`, retenção de worktree —, então o que falta é aplicá-lo ao meio do laço. NÃO se espera bloqueando: o ToolLoop roda dentro do callback do GenServer, e bloquear ali trava a caixa de mensagens que traria a própria decisão |
| [0053](0053-dev-lead-e-paralelismo-autorizado.md) | Dev Lead como área, e paralelismo autorizado pelo usuário. Hoje sobe um dev por módulo mais um extra por aceite de um clique, sem teto de sessão nenhum: N módulos × 2 agentes, e o gasto sobe sem que ninguém autorize. A 14d decide que quem avalia quantos agentes valem a pena é o LEAD, e que passar de dois na sessão vira `proposed_action` pelo mesmo pipeline de toda ação com efeito externo. O Dev Lead não existia e era proibido, junto com as áreas dinâmicas via `module_map` e o aparato genérico de áreas — os três cortes caem juntos porque os membros da área de dev são um por módulo do `module_map`, decididos pelo Arquiteto e diferentes em cada projeto: não há como hardcodá-los como `qa` e `infra`. `agent_areas`/`agent_area_members` entram por projeto, com `max_parallel` default 2 configurável por lead; o contrato externo dos gates não muda e a suite da Fase 4 verde sem modificação é o critério de aceite. A Anamnese propõe subir o teto quando a autorização vira recorrente, e nunca sobe sozinha — automatizar seria o produto elevando o próprio teto de gasto |
| [0054](0054-gates-como-registro-declarativo.md) | Gates como registro declarativo em `docs/gates.yml`. Os gates do fluxo existiam e tinham sido validados por execução real, mas estavam implícitos — espalhados entre prompt de agente, regra pura, use case, teste e CI —, e a lição da Fase 10/13 (métrica extraída por script, nunca anotada) não tem como ser aplicada ao que não é enumerável. Este ADR é a metade de cima do [0048](0048-decisao-no-log-e-a-ordem-do-gate.md): aquele tornou a decisão OBSERVÁVEL no event log, este torna o conjunto ENUMERÁVEL. Cada gate declara `evidencia: event_log | teste | ci` com o localizador, porque nem toda prova está no log — `merge-protegida` é um teto em regra pura que não emite evento próprio (o que o garante é teste) e `backmerge` é CI com estado em `.release/gate.json`; rebaixá-los a `warn` seria mentir sobre as travas mais duras do produto. O filtro de payload entra junto do tipo porque `qa-verificada` e `secops-segura` gravam o MESMO `pr.gate_changed`, e o mesmo tipo sai na abertura do gate sem `veredito` — sem o filtro, abertura contaria como passagem. Entram também os gates do REPOSITÓRIO (ADR 0030) — incluir o `backmerge` e deixar `pr-police`, `approval-ladder` e `promotion-check` de fora seria arbitrário, são a mesma família, e `aprovacoes-da-escada` é o único gate de CI com decisão humana: o que ele mede é gente aprovando. Enumerar já rendeu dois dividendos antes de medir qualquer coisa — achou o julgamento sobre PR de infra, que a lista esquecera, e achou que `promotion-check` é check required SEM spec própria |
| [0055](0055-escopo-de-caminho-na-politica-de-terminal.md) | Escopo de caminho na política de terminal. A escada de aprovação de `terminal` foi MEDIDA como inviável: 15 turnos, 209.031 tokens de entrada contra 4.546 de saída, 14 ações e ZERO linha escrita para a task, porque quase todo turno terminou esperando um clique. Dois defeitos independentes explicam o "pergunta sempre": o agente emite `cd <caminho> && <verbo>` e `cd` não está em allow nenhum, e comando composto exige que TODOS os segmentos casem (`decide.ts:201`), então o allow semeado quase nunca é alcançado; e "Sempre permitir" grava o comando LITERAL inteiro, que nunca volta a casar porque o casamento é por prefixo de token — o escape hatch não escapa. O que o usuário pede, "sempre desde que na pasta do projeto", é hoje inexpressável: só `write_file` tem noção de caminho, `terminal` não. A execução expôs o agravante: `/workspace` dentro do executor é o monorepo do PRÓPRIO Brabo, e o dev agent do hello-limpo chegou a propor ler `git_executor.ex` e `context_builder.ex` — não existe fronteira de projeto, e o alcance é maior que o projeto. Escopo resolve por REALPATH e não por prefixo de string (um `..` escaparia), PERMITE sem isentar (rede e instalação continuam pedindo; estar na pasta não torna `curl` piped para shell seguro), `deny` continua vencendo primeiro, e fora do escopo é `require_approval` e não `deny`. É política, não isolamento: enquanto o monorepo estiver montado no container que executa, a fronteira depende de a política acertar |
| [0056](0056-o-engine-trabalha-em-repositorio-remoto.md) | O engine trabalha em repositório remoto. `get_local_repo_path/1` devolve `unsupported_provider` para tudo que não é `local`, e quatro consumidores param junto — worktree do dev, executor de terminal, o diff que QA e SecOps julgam, e o contexto de projeto. A assimetria é a raiz: a api fala GitHub por HTTP (criou o repo, commitou, criou as branches) e o engine trabalha no sistema de arquivos, só conhecendo bare repo local; projeto remoto faz a metade conversacional e para na de construção. É o que impede a FASE 13b de existir como escrita. O encanamento de git já é genérico — `init + remote add + fetch + checkout` não se importa se a origem é caminho ou URL —, então o que falta é CREDENCIAL. O engine não recebe a chave mestra: pede o remoto de trabalho à api pelo canal `/internal/*` que já existe, no momento em que precisa. E o token NUNCA entra no `.git/config`, decisão que é consequência direta do [0055](0055-escopo-de-caminho-na-politica-de-terminal.md): escrever `https://x-access-token:TOKEN@…` no `origin` põe o segredo em texto puro DENTRO da pasta onde a RN-075 acabou de dar leitura auto-aprovada ao dev agent — um `cat .git/config` devolveria o token, que viajaria ao provider de LLM no turno seguinte. O escopo protege contra ler para fora, não contra um segredo que o produto colocou dentro. `origin` fica com a URL limpa e a autenticação é injetada por invocação. Credencial é a do OWNER (RN-058), `local` segue idêntico, e falha de credencial reprova com origem `infra` |
| [0057](0057-o-gate-espera-a-aprovacao.md) | O agente de gate espera a aprovação, como o dev espera. O [ADR 0052](0052-dev-agent-espera-aprovacao-no-meio-do-laco.md) fez isso para o dev agent; os agentes de GATE ficaram de fora, e ninguém percebeu porque nenhuma execução chegava até eles — a FASE 13b chegou. Na 6ª execução real o `qa-automacao` rodou `ls -la && find … | head -50`, comando composto com um segmento fora do `allow`: o `ToolLoop` suspendeu corretamente, e o `QaLeadServer` classificou a suspensão como *desfecho inesperado* com `failureOrigin: infra`. Duas coisas erradas de uma vez — a task era bloqueada por uma decisão que ninguém tomou, e a origem era mentira, contra a regra do [0020](0020-destravar-gates-qa-secops.md) de que origem é nomeada e nunca obtida por eliminação. O subagente ganha um terceiro desfecho, `{:awaiting, …}`, com o `ctx` inteiro viajando junto; o Lead, que já era GenServer por projeto, assina o `Wake` pelos SUBAGENTES (é o ator que propôs a ação que chaveia o `task.action_settled`), guarda o estado em voo e continua a área do ponto em que parou. `rodar_ativas` era um `Enum.map` sem como parar no meio, e virou recursão sobre as restantes — é essa forma que permite suspender entre duas delegações sem perder a primeira. Enquanto pendente, a área não consolida, não emite veredito e não bloqueia task: ela espera, e o clique destrava em vez de chegar tarde. Recusa também retoma, com o motivo no lugar do resultado. NÃO elimina a aprovação, e não deveria — o gate roda comandos no worktree do dev, e trocar a parede por um buraco não é resolver; os achados X, Z e AD seguem abertos, e o que muda é a parede virar fila. Limite declarado: restart no meio da espera perde o laço, porque o `pendente` vive na memória do lead |
| [0058](0058-csp-fechado-na-api-e-escopo-de-projeto-contido.md) | CSP fechado na api, e o escopo de projeto contido na raiz. Revisa o item 7 do [0027](0027-fase5-backup-hardening-release.md), que decidira `contentSecurityPolicy: false` porque "a api serve JSON e o CSP é da web" — verdade que não é o ponto: a alternativa a um CSP GENÉRICO não é cabeçalho nenhum, é um CSP ESPECÍFICO, e para quem só serve JSON o específico é `default-src 'none'`, que custa zero comportamento. Dois caminhos põem a resposta da api fora do alcance do CSP da web: navegação DIRETA a uma rota (link colado, redirect), renderizada na origem da api, e `frame-ancestors`, que só vale no documento emoldurado. Fora de produção o perfil afrouxa só o que o Swagger UI de `/docs` exige, sob a MESMA condição que monta o Swagger — se um dia ele subir em produção, o CSP acompanha em vez de barrá-lo calado. As opções saem do literal do `main.ts` para `security-headers.ts` pelo motivo que já movera o `cors-origins.ts`: no boot elas não eram testáveis, e a prova tem que ser uma requisição de verdade, porque `false` e um objeto de diretivas são os dois "configuração válida" e só diferem na resposta HTTP. Junto vem a contenção do `projectId`: ele chega do parâmetro de rota com o percent-encoding já decodificado pelo Express, então `..%2F..%2Fetc` virava `../../etc` e escapava da raiz — atingindo não só a leitura e ESCRITA do `permissions.json` como o escopo que AUTORIZA terminal pelo [0055](0055-escopo-de-caminho-na-politica-de-terminal.md), isto é, a política apontando para o lugar errado. A validação fica onde a raiz é derivada, e não em cada chamador, pelo mesmo motivo que fez a função existir: checagem duplicada é checagem que um dia diverge. Registra também as sete dispensas do CodeQL com a razão de cada uma — entre elas o `js/insufficient-password-hash` sobre um HMAC de `state` de OAuth, onde não há senha e hash lento seria erro |
| [0059](0059-segredo-do-state-de-oauth-sem-default.md) | A chave que assina o `state` do OAuth de git não tem default. Nasce de uma das dispensas do [0058](0058-csp-fechado-na-api-e-escopo-de-projeto-contido.md): dispensar o `js/insufficient-password-hash` estava certo — ali não há senha, o "password" é `GIT_OAUTH_STATE_SECRET`, chave HMAC de servidor, e HMAC-SHA256 é o primitivo certo. O que o argumento não considerou é que uma chave de servidor **com valor padrão conhecido não assina nada**, e era esse o caso: os dois pontos de leitura faziam `process.env.GIT_OAUTH_STATE_SECRET ?? 'dev-oauth-state-secret-change-me'`. Mesmo modo de falha que o `WEB_ORIGIN` já tivera fechado no `cors-origins.ts`, e pior por dois motivos — o valor é PÚBLICO (está no `.env.example` de um repositório open source, não é default fraco, é segredo publicado), e o que ele protege é o `state`, único obstáculo a forjar o callback de uma rota que precisa ser pública: com a chave conhecida qualquer um assina `{projectId, userId, provider}` à escolha e faz o callback gravar, no projeto daquele payload, o token de git obtido do provider. O detalhe que decide o formato da correção é que o `docker-compose.prod.yml` supria o literal como fallback, então no caminho REAL de erro a variável estava definida — exigir só "não vazia" não teria pego nada, e por isso o literal de exemplo é rejeitado explicitamente, junto com um piso de 16 caracteres que transforma em verificação o "fraco = CSRF" que a tabela de configuração sempre disse. A checagem roda no BOOT e não no primeiro uso: uma api que só falhasse quando alguém tentasse conectar git passaria semanas de pé aceitando `state` assinado com chave pública. Resolução em função única pelo motivo da [RN-092](../business-rules.md#rn-092) — eram duas cópias do mesmo literal, e divergindo elas o callback recusaria todo `state` legítimo. Quebra deliberada: produção sem a variável deixa de subir, o que é o objetivo e não efeito colateral. Os quatro segredos irmãos do mesmo compose (`AUTH_JWT_SECRET`, `BRABO_SERVICE_TOKEN`, `CREDENTIALS_MASTER_KEY`, `SECRET_KEY_BASE`) ficaram declaradamente de fora, no backlog — fechados depois pela mesma regra, sem ADR novo, em [RN-114](../business-rules.md#rn-114) |
| [0060](0060-superficie-de-leitura-de-codigo.md) | A superfície de leitura de código da aba Code, contida e com orçamento. A 26a pôs `listTree` e `getPullRequestDiff` no contrato e instalou a trava que reprova operação sem consumidor; este ADR é o consumidor, e decidir escrevê-lo obrigou a resolver três coisas que nenhuma rota anterior tinha enfrentado, porque nenhuma recebia CAMINHO DE ARQUIVO do cliente. **Onde mora a busca:** fora do contrato. Nenhum dos três providers a tem — GitHub e GitLab têm code search de plataforma, com semânticas próprias, e o `LocalGitProvider` é um bare repo —, então declará-la seria ou uma 13ª operação com capability `false` no local (aba que some num provider) ou o vocabulário de uma plataforma vazando para dentro do contrato normalizado que existe para impedir exatamente isso; ela fica composta na aplicação, sobre `listTree` + `getFileContent`. **Quanto ela pode gastar:** três orçamentos (diretórios percorridos, arquivos abertos, casamentos devolvidos) mais cache de TTL curto, porque é a única leitura cujo custo cresce com o TAMANHO do repositório e não do pedido, e quem paga é a credencial do OWNER (RN-058/RN-082) — sem teto é a família de defeito dos 3.824 req/min do dashboard, com o `429` virando tela branca. Varredura em LARGURA: cortada no meio, ela entrega os arquivos rasos, que é onde se olha. **Como o caminho é contido:** uma função só, no arquivo da [RN-092](../business-rules.md#rn-092), reusando as primitivas de escopo do [0055](0055-escopo-de-caminho-na-politica-de-terminal.md) — e devolvendo o caminho NORMALIZADO, porque devolver o original permitiria conferir uma string e mandar outra. O vetor não é ler o arquivo errado: em provider remoto o caminho vira segmento de URL da API e um `..` TROCA DE ENDPOINT com o token do owner na mão. O CodeQL vai continuar apontando, e o preço é o mesmo já aceito na PÓS-FASE 15 — duplicar a checagem calaria o alerta e criaria quatro cópias que um dia divergem. As quatro rotas são `GET`/`role:viewer` e o controller não tem UM verbo de escrita: ler não é efeito externo e não vira `proposed_action` (encheria a fila de ruído), e quando a escrita vier ela nasce como uma — a ausência de verbo é o que torna essa fronteira verificável em vez de intenção |
| [0061](0061-tipo-da-sessao-na-criacao.md) | O tipo da sessão é dado da criação, e a execução continua sendo evento. Toda sessão nascia igual, e chegar ao Criativo exigia abri-la primeiro e DESCOBRIR depois um botão na barra de topo — foi assim que o usuário relatou o problema. O risco não é o campo novo: é que o produto JÁ sabia distinguir uma sessão que executa, por derivação, e `findActiveExecutionSession` procura a sessão `active` que carrega `execution.activated` (é o que faz reativar cair na sessão onde os dev agents já estão, achado #11 do primeiro dogfooding). Gravar um tipo cria uma SEGUNDA fonte, e duas fontes divergem — aqui, sobre onde os dev agents escrevem. Derivar tudo do log não serve, e o motivo é temporal: a intenção existe ANTES de qualquer evento, no clique que abre a sessão, e a sessão criativa que ninguém ativou não tem evento nenhum a derivar. A decisão é que as duas coexistam respondendo perguntas diferentes — `kind` é INTENÇÃO de criação e imutável, o evento é ESTADO de execução, e `findActiveExecutionSession` NÃO passa a olhar `kind` —, sob a regra que as impede de brigar: `execution.activated` em sessão `consultiva` é 409, nunca conversão silenciosa. A trava mora no FUNIL (`AppendSessionEventUseCase`), porque a rota do usuário e a `/internal/*` do engine passam por ele e travar no caso de uso deixaria o outro aberto; e roda antes do `incrementSeq`, para tentativa recusada não consumir `seq`. O campo é obrigatório em todo ponto de criação, inclusive nos quatro internos: opcional faria os chamadores herdarem o default calados. O DEFAULT da coluna é `consultiva` (o tipo que pode menos) e o BACKFILL é `criativa`, na direção oposta e pelo argumento do [0046](0046-promocao-de-story-com-autoridade-do-usuario.md) — toda linha existente é anterior à distinção, e algumas são as sessões em que os dev agents trabalham agora. Junto vêm o nome amigável, que se SOMA à hashtag e nunca a substitui (a hashtag é o que se cola numa URL; nome de pessoa não é único), e a saída da tela de sessão, que não tinha nenhuma. O preço aceito: a pergunta "esta sessão executa?" passa a ter dois lugares onde procurar |
| [0063](0063-duas-audiencias-para-o-mesmo-gasto.md) | Duas audiências para o mesmo gasto: a fatura do owner e o consumo do membro. O pedido era uma aba de resumo de gastos no espírito do *activity* do OpenRouter, e o dado nunca foi o problema — `token_usage` tem provider, modelo, ator, tokens e o preço CONGELADO desde o [0042](0042-catalogo-vivo-ciclo-de-vida-do-modelo-e-preco-auditavel.md); faltavam as AGREGAÇÕES (não havia por modelo, por projeto dentro do workspace, por sessão nem por pessoa). A dificuldade é a colisão entre duas regras já existentes: pela [RN-058](../business-rules.md#rn-058) a chave que o agente gasta é a do OWNER, e pela [RN-060](../business-rules.md#rn-060) a fatura dessa chave é dele e só dele — então "quanto EU gastei" é, literalmente, pedir fatia de conta alheia. As duas saídas fáceis são erradas: abrir o relatório de credencial filtrado revoga a RN-060 pela porta dos fundos (a resposta seguiria falando de provider e chave, e somar as linhas reconstrói a conta), e não mostrar nada deixa a única pessoa proibida de ver o consumo dela ser justamente ela. A decisão separa as PERGUNTAS: a do owner é por CREDENCIAL (provider, chave, fatura) e continua exigindo `owner`, reaproveitada inteira em vez de reescrita; a do membro é por ATOR, em tokens e custo estimado, SEM quebrar por provider nem por credencial. O ator sai do token autenticado e não há parâmetro onde escrever o id de outra pessoa — "membro não vê linha de outro ator" vira propriedade da assinatura, não checagem que alguém esquece; provado por teste, e a mutação que remove o filtro o mata. O eixo de provider não EXISTE na agregação nova (`sumGroupedBy` tem `model`/`project`/`actor`/`session`/`day` e nada mais): a ausência é o que impede a visão do membro de ganhá-lo por descuido, e pelo mesmo motivo dois providers servindo o mesmo nome de modelo caem numa linha só. Agente não entra na conta do membro porque `token_usage` registra quem gastou, não quem mandou gastar. Sem lib de gráficos — duas formas, uma série cada, `<rect>` inline resolve —, e a série diária vem DENSA da api, senão três chamadas em três semanas viram três barras coladas. Sem migration: medido com 525 mil linhas, as consultas saem em 55 ms e 38 ms por *seq scan*, e um índice em `created_at` as levaria a 32 ms e 19 ms — o ganho está registrado e o índice entra depois |
| [0064](0064-escopo-de-area-na-cascata-e-o-binding-de-agente-global.md) | Escopo de área na cascata de modelo, e o binding de agente global. O pedido: leads de área e seu subgrupo compartilhando um modelo vigente, com o agente podendo divergir — padrão herdável, decisão já tomada pelo usuário. A cascata (`sessão > agente > projeto > workspace`) ganhou `area` entre agente e projeto, e responder ONDE levantou uma incoerência que precisou de decisão antes de codar: o binding de `agent` era GLOBAL (`scope_id` = slug puro, `:projectId` da rota IGNORADO de propósito desde a Fase 9a) e `area` é POR PROJETO desde o ADR 0053 — um escopo por projeto ACIMA de um escopo global faria o mesmo agente resolver modelos diferentes só onde existisse área, comportamento dependente de acidente de dados. Duas saídas mutuamente exclusivas: o binding de agente virar por projeto (muda comportamento existente), ou a área ficar ABAIXO do agente (contradiz "padrão herdável", porque quase todo binding hoje É de agente). Escolhida a primeira: `scope_id` de `agent` e `area` virou COMPOSTO — `<projectId>:<slug|chave>`, sem tabela nova, porque nem UUID de projeto nem slug/chave contêm `:` — e o formato antigo passou a ser RECUSADO na escrita, não aceito e ignorado, para não gravar um binding que a cascata nunca mais encontraria. A migração 0040 espalha cada binding global existente para uma linha por projeto (preserva o que cada projeto resolvia, porque a linha global nunca guardou dono) e apaga o formato antigo. O nível novo entra na MESMA revalidação de capability do ADR 0041 (RN-041/043) e também exige tool calling (RN-040) — área só é lida por agente, nunca por chat humano. "Voltar a herdar" (RN-102) é DELETE, nunca cópia do modelo de baixo para cima: copiar viraria cópia que diverge sozinha na próxima mudança da área. Mudar o modelo da área exige `maintainer` — a mesma régua do teto de paralelismo (RN-083), porque alcança lead e subagentes de uma vez |
| [0065](0065-container-por-projeto-a-fronteira-deixa-de-ser-politica.md) | Container por projeto: a fronteira deixa de ser política. Revisa o [0055](0055-escopo-de-caminho-na-politica-de-terminal.md), que declarara de si mesmo a pendência: escopo de caminho é POLÍTICA, não isolamento — enquanto o monorepo do Brabo estiver montado no mesmo container que executa comandos, a fronteira depende da política acertar, e symlink de dentro apontando para fora não é detectado. Os achados Z e AD (allowlist de verbo não converge — verbo, forma e invocação são espaços distintos) reforçam a mesma conclusão da FASE 13: o caminho para autonomia não passa por afrouxar política, passa por trocar a fronteira. Decisão do usuário, textual: cada projeto ganha container próprio, dentro dele o agente é LIVRE (ler, escrever, instalar, buildar, testar, rodar), e o Code só libera depois que o ARQUITETO decide qual imagem sobe — ele é quem sabe qual runtime a stack pede. A imagem nasce ARTEFATO (`artifact.project_image` no event log, versionado, sem tabela — revisar é emitir de novo), com tag OCI explícita (`latest` recusado) e `rationale` obrigatório; o portão (RN-105) faz a aba Code responder 409 enquanto `sem_decisao`, na MESMA função que já contém o caminho (RN-092), nunca duplicada nas quatro rotas. A fronteira que sobra é FORA: `git push`, PR e deploy não saem pelo terminal — nem dentro do escopo do projeto —, e a regra é `deny`, não `require_approval`, porque "sempre permitir" grava o padrão em `allow` e um clique reabriria a porta para sempre; a mensagem redireciona para a ação TIPADA (`git_push`/`git_merge`/`pr_open`), que já é o caminho real do dev agent. Rede é decidida UMA VEZ no artefato (`network: none` default, `egress` autorizado pelo usuário) e não comando a comando — repetir o allowlist para egresso seria repetir o erro que Z e AD provaram não convergir; gasto (cpus/memória/pids) tem teto que RECUSA em vez de rebaixar em silêncio. Corte HONESTO e declarado: o ciclo de vida do container (provisionar, reciclar, limpar, o que sobrevive a restart) fica para a fase com slot de migration — estado de container é mutável e pede tabela, e um container que sobe sem reciclar é pior que nenhum. Consequência: a metade "dentro é livre" da política de terminal AINDA NÃO mudou; o 0055 continua valendo como está até a parede subir de verdade |
| [0066](0066-nome-de-pasta-legivel-do-workspace.md) | Nome de pasta legível do workspace. Revisa o [0055](0055-escopo-de-caminho-na-politica-de-terminal.md): `projectScopeRoot` passa a receber `workspace_dir_name`, não mais o `projectId` cru, preservando a garantia de que api e engine concordam sobre a raiz do projeto. Pedido do usuário: pasta física legível (`<slug>-<8 chars do id>`), com id único — `PROJECT_WORKSPACES_ROOT` é UMA raiz para a instância inteira, e slug sozinho não é globalmente único. Entre as duas opções apresentadas (symlink de menor risco vs. renomear a pasta de verdade), o usuário escolheu explicitamente a segunda. O id nasce em CÓDIGO (`crypto.randomUUID()` no `CreateProjectUseCase`, não mais `defaultRandom()` do Postgres), porque o nome da pasta precisa dele ANTES do `INSERT`. Projeto criado antes desta migração manteve a pasta física que já tinha — o backfill grava `workspace_dir_name = id` e NUNCA renomeia diretório nenhum, e o nome é CONGELADO na criação: editar o slug depois não recalcula a pasta. Um trigger `BEFORE INSERT` (o primeiro do histórico de migrations do produto) aplica o mesmo fallback `id::text` para insert que chegue sem o campo — rede de segurança, não o caminho principal, e o que evita reescrever mais de cinquenta specs de teste que inserem `projects` sem conhecer o conceito. O engine lê a MESMA coluna (`Engine.Projects.Project.workspace_dir_name/1`) para resolver `Workspace.workspace_dir/1`, nunca recomputando o nome — as duas derivações são, na prática, a mesma leitura contra o mesmo banco. Fora do hot path do laço de ferramentas (que já recebe `ctx[:workspace_root]` resolvido uma vez, na criação do worktree do agente); `WorktreeCleanup` trocou de varrer o disco para consultar `Project.all_workspace_dirs/0` numa query só, porque a pasta deixou de ser o `project_id` e não há como voltar um para o outro sem consultar |
| [0067](0067-o-gate-sobrevive-ao-restart.md) | O gate sobrevive ao restart. O [0057](0057-o-gate-espera-a-aprovacao.md) já declarava "restart no meio da espera perde o laço" como limite conhecido, mas investigar de novo achou uma SEGUNDA janela, distinta da suspensão em `{:awaiting, ...}`: `DevAgentServer.correct/3` e `Dispatcher.run_secops/2` são chamada direta em memória, feitas DEPOIS de `record_gate_verdict` já ter avançado o `gate_status` de forma durável na api — um crash entre as duas prende a PR pra sempre, e é a janela mais FÁCIL de acontecer, não a mais rara. `gate_states` (schema `engine`, chave `{project_id, task_id, gate}`) grava dois `step`: `in_progress` (nenhum veredito gravado ainda — nenhuma retomada cirúrgica do `ctx`, que não sobrevive a restart, mas reiniciar a área do zero é seguro porque a api rejeita `record_gate_verdict` pro gate que já não é mais dono do `gate_status`) e `dispatch_pending` (veredito já gravado, só falta a chamada em processo). `Engine.Gates.GateRescuer` varre linhas paradas há mais de 15 min (generoso de propósito — o ToolLoop de QA roda legitimamente até 60 iterações, e resgatar cedo demais duplicaria um ciclo só lento), chamado no boot e por tick Oban auto-reagendado, reusando o mesmo mecanismo de fila que o resto do engine já usa. Duas guardas contra duplicar trabalho: `Registry.lookup` local antes de qualquer resgate, e o próprio limiar de staleness como segunda linha pro que a primeira não cobre (réplica remota, `Registry` local ao nó, mesma ressalva do `Engine.Dev.Wake` desde o ADR 0045) — o pior desfecho de uma corrida residual é trabalho duplicado e barato, nunca dado inconsistente |
| [0068](0068-diagrama-c4-do-arquiteto.md) | Diagrama C4 (Context + Container, Simon Brown) do Arquiteto, renderizado na Visão Geral. `artifact.c4_diagram` sem tabela, mesmo desenho do `artifact.project_image` (ADR 0065): o Container level é DERIVADO do `module_map` vigente pelo caso de uso (nunca redigitado pelo modelo — evita divergência silenciosa entre diagrama e mapa real), e só o Context (nome do sistema + atores externos) vem do tool call `create_c4_diagram`. `mermaid` entra como dependência de RUNTIME nova do `apps/web` (decisão do usuário) — primeira do tipo; isolada atrás de `lib/mermaid-render.ts` com `import()` dinâmico, confirmado por `vite build` que só o entrypoint carrega eager e os chunks pesados do Mermaid (`cytoscape.esm` 435 KB, `katex` 258 KB, usados só por OUTROS tipos de diagrama) ficam sob demanda. CSP fechado do ADR 0058 confirmado intacto: nenhum chunk do Mermaid usa `eval`/`new Function`, e `style-src 'unsafe-inline'` já cobria o `<style>` que o SVG pode embutir |
| [0069](0069-fusao-condicional-do-handoff-com-a-ativacao-de-execucao.md) | Fusão condicional do aceite do handoff com a ativação de execução. O card do Dev Lead tinha dois cliques porque são duas autorizações deliberadamente distintas (RN-137): aceitar exige `developer`, ativar exige `maintainer` porque quem ativa vira `session.createdBy` e resolve o papel EFETIVO das PRs que os dev agents abrem depois. O pedido não baixa essa exigência — para quem já é `maintainer`/`owner` (lido do mesmo `useCurrentWorkspaceWithRole()` que já autoriza o "Auto mode" e Aprovações/Configurações), o segundo clique não protegia nada, só repetia uma decisão que o papel já autorizava; `handleAcceptHandoff` passa a encadear a MESMA `handleActivateExecution` que o botão "Ativar execução" já chama (RN-137), reusando o tratamento de erro dela para não confundir "aceite falhou" com "ativação falhou depois de aceitar com sucesso". Backend intocado — `RequireRole('maintainer')` continua igual, é decisão só de `apps/web`; `developer` mantém o fluxo de hoje, com os dois botões separados |

| [0070](0070-observabilidade-no-compose-local.md) | Observabilidade no Compose local: métrica e log, sem trace. O [0026](0026-fase5-observabilidade-e-graceful-shutdown.md) montou Prometheus, Loki, Tempo, Alloy, Collector e Grafana **só para Kubernetes** — o critério de aceite dizia "no Grafana local" e "local" ali era o cluster k3d. A consequência prática é que quem desenvolve com `pnpm dev` tem a instrumentação inteira funcionando (a api expõe 102 séries, o engine expõe `oban_queue_depth`, os três carimbam `trace_id` no log) e NADA disso é observável, porque ver um painel exigia subir um cluster que por desenho não coexiste com o `pnpm dev` (mesmas portas, ADR 0025). O overlay `docker/docker-compose.observability.yml` é opt-in, não parte do `pnpm dev`: são quatro containers a mais numa máquina que já roda Postgres, Ollama e três apps, e quem não está investigando não deve pagar por eles. Entrega as duas telemetrias que NÃO precisam de coletor — métrica é scrape, log é lido do stdout, exatamente a separação da decisão 9 do 0026 — e deixa trace de fora, sem definir `OTEL_EXPORTER_OTLP_ENDPOINT`, porque o [0035](0035-observabilidade-legivel-e-trace-sem-coletor.md) já estabeleceu que não exportar é o estado normal em desenvolvimento e apontar para endereço inexistente só produz erro de exportação a cada turno. Os artefatos são os MESMOS do cluster e não uma cópia: o Compose monta `deploy/k8s/observability/dashboards/` direto e os UIDs de datasource são idênticos, porque dashboard referencia datasource por UID e cópia de dashboard diverge no primeiro painel corrigido de um lado só; pelo mesmo motivo o scrape emite os rótulos `app`/`pod` que os painéis agrupam e o coletor emite o `app` que o Alloy do cluster deriva de `app.kubernetes.io/name`. O parsing do log é por regex e não `stage.json` porque em dev o `pino-pretty` desenha a árvore legível e o engine usa `PrettyLogFormatter` — decisão boa que não se troca para agradar o coletor; o coletor é que se adapta. Só `api`, `engine` e `web` vão para o Loki: Postgres e Ollama são ruído, e o próprio stack de observabilidade fica de fora porque roda no MESMO projeto do Compose e ingeriria o próprio log num laço que se alimenta sozinho. `trace_id` é metadado ESTRUTURADO e nunca rótulo (cardinalidade), o que exigiu `allow_structured_metadata` e schema `v13` numa config própria do Loki. Registra três modos de falha silenciosa que custaram tempo, todos com a mesma assinatura — config válida, zero erro no log, dado errado no painel: `stage.replace` substitui GRUPOS DE CAPTURA e sem parêntese não substitui nada; escape de `\x1b` em string do River é processado antes de a regex compilar, e `[[:cntrl:]]` não depende dessa camada; e template do Go com chave ausente renderiza o literal `<NO VALUE>`, que virou um valor de nível no seletor |

## A convenção

- **Um arquivo por decisão**, em `docs/adr/NNNN-titulo-curto.md`, com
  numeração sequencial de 4 dígitos. **Sem reuso de número**, nem quando um ADR
  é superado — o próximo é **0071**.
- **Três seções, só elas:** **Contexto** (o problema ou a força que motivou),
  **Decisão** (o que foi decidido), **Consequências** (os trade-offs aceitos e
  o que fica para depois).
- **Sem template de terceiros** — nada de Nygard, MADR ou variantes. As três
  seções bastam.
- **Em pt-BR**, como o resto do projeto.
- **ADR aceito não é editado.** Um superado por decisão posterior continua como
  está; o ADR novo referencia o antigo e explica o que mudou.

O que dá valor a estes ADRs é registrarem o que **não** funcionou: o 0020
documenta nove execuções falhas seguidas, e o 0027 registra uma justificativa
que foi revista dentro da mesma sessão que a produziu. Consequência ruim
omitida é a parte que faz falta seis meses depois.
