{"responses":{"201":{"description":"","content":{"application/json":{"schema":{"type":"object","properties":{"status":{"type":"object","example":"provisioned","description":"Since `protect_branches` is the LAST step, acknowledging its failure closes the bootstrap — the project becomes reachable from the dashboard."}},"required":["status"],"title":"ReconhecerFalhaDeProtecaoResponseDto"}}}},"401":{"description":"No token, expired token, or invalid signature."},"403":{"description":"Insufficient role on the project."},"404":{"description":"Project does not exist."},"409":{"description":"The bootstrap did not fail, or it failed on a step before protection — moving on there would leave the project without a usable repository."},"429":{"description":"Rate limit per user or per IP."}}}