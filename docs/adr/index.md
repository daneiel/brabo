---
id: adr-index
title: Architectural decisions (ADR)
sidebar_label: ADR index
sidebar_position: 0
description: Brabo's 103 architectural decision records, grouped by phase, with what each one decided.
keywords: [ADR, architectural decision, architecture, history]
---

# Architectural decisions

An ADR records **why** something was decided, at the moment it was decided.
They are **immutable**: no ADR is edited after being accepted. When a
decision changes, a new ADR is born that supersedes the previous one — that's what happened
with [0018](0018-noop-dev-agent-modo-de-execucao.md), which records the
replacement of the NoopDevAgent decided in [0011](0011-infra-dev-agents-worktrees-merge-lock.md).

Practical consequence: **an ADR can describe a state that is no longer
current.** For the current state, use [Business rules](../business-rules.md),
[Architecture](../architecture.md) and the [Runbook](../runbook.md). For the
reasoning, come here.

Of the 61, all are **accepted** except those still waiting for implementation — today [0053](0053-dev-lead-e-paralelismo-autorizado.md), [0054](0054-gates-como-registro-declarativo.md) and [0056](0056-o-engine-trabalha-em-repositorio-remoto.md), marked **proposed** in the file itself. [0055](0055-escopo-de-caminho-na-politica-de-terminal.md) was accepted once it was implemented in Phase F. No check verifies this sentence: it aged saying 48 when there were already 54, and again 58 when there were already 59, and it's the argument of ADR 0029 (`gerar > verificar > lembrar`) confirming itself against itself.

## Phase 2 — Git

The contract that makes Local, GitHub and GitLab interchangeable.

| # | decision |
|---|---|
| [0001](0001-git-provider-contract-shape.md) | Format of the GitProvider's normalized contract: nine operations and declared `capabilities`, validated by a single contract suite |
| [0002](0002-git-error-normalization.md) | Error normalization: six specific classes, **with no** common base class — the abstraction was considered and rejected for not paying for itself yet |
| [0003](0003-git-provider-retry-policy.md) | Automatic retry **only on reads, never on mutations**, with Full Jitter and 4 attempts |
| [0004](0004-git-credential-registration.md) | Registration of git credentials: dedicated enum in the database, united with LLM providers only in type |
| [0005](0005-repo-bootstrap-idempotent-steps.md) | Gitflow bootstrap in six idempotent, resumable steps; `skip` is success |

## Phase 3 — Harness and first agents

The wrapper that makes agent behavior reproducible.

| # | decision |
|---|---|
| [0006](0006-prompt-assembler-context-harness.md) | **Deterministic** context assembly: ordered layers, budget per layer, predictable truncation |
| [0007](0007-toolloop-tools-context-manager.md) | ToolLoop, tool catalog and ContextManager — the loop with a real LLM |
| [0008](0008-agente-criativo-conversacional-handoffs.md) | Conversational Creative agent (chat stops being a stateless echo) and the foundation of handoffs |
| [0009](0009-agente-po-backlog-rastreabilidade.md) | PO, three-level backlog and rule → story → task traceability; readiness becomes a domain validation |
| [0010](0010-agente-arquiteto-adr-pr-module-map.md) | Architect: ADRs per real PR, `module_map` and cross-validation |

## Phase 4a — Execution and gates

Where the system starts touching real code — and where limits are born.

| # | decision |
|---|---|
| [0011](0011-infra-dev-agents-worktrees-merge-lock.md) | Worktrees per dev agent, git executors and the **merge lock**: a protected branch is never auto-approvable |
| [0012](0012-dev-agent-real-toolloop-orcamento-bloqueio.md) | Real DevAgent: ToolLoop in the worktree, budget per task, `ReportDone` requiring a green suite, blocking with diagnosis |
| [0013](0013-gates-qa-secops-pr.md) | QA and SecOps gates with an immutable-order state machine |
| [0014](0014-infra-agent-painel-phoenix.md) | **Proposal-only** InfraAgent (proposes via PR, never applies) and a live team panel over Phoenix channels |
| [0017](0017-lock-de-workspace-e-monitor-de-dev-agents.md) | Workspace initialization lock, dev agent Monitor and **cap inheritance** for subagent corrections |
| [0018](0018-noop-dev-agent-modo-de-execucao.md) | NoopDevAgent stops being scaffolding and becomes a **permanent execution mode** |
| [0019](0019-destravar-dev-agent-adr-por-modulo-orcamento-projeto.md) | Unlocking the real DevAgent: writing, terminal, ADR per module and budget per project |
| [0020](0020-destravar-gates-qa-secops.md) | Unlocking the gates: gitleaks on the **working tree** (not just the diff), verdicts validated as an artifact, tool call recovery from text |
| [0021](0021-fechamento-4a-infra-e-painel.md) | Closing 4a: an infra gate that really validates, and a panel that tells the truth |

> **Acceptance criterion still open.** [0020](0020-destravar-gates-qa-secops.md)
> explicitly records that the gates demo is **"CLOSED, but NOT
> DETERMINISTIC"** — it depends on the judgment of a local 7B model and doesn't serve
> as a regression test. [0021](0021-fechamento-4a-infra-e-painel.md) has
> a section "Acceptance criterion status: NOT CLOSED". Both are honest
> on purpose; don't read them as finished.

## Phase 4b — Psychologist and Anamnesis

The loop that makes the team improve.

| # | decision |
|---|---|
| [0015](0015-psicologo-real-toolloop-hipoteses-evidencia.md) | Real Psychologist: hypotheses with **evidence pointing to real events**, and light/heavy triage by cost |
| [0016](0016-anamnese-proficiencia-patches-instrucao.md) | Anamnesis: a **closed** catalog of competencies, versioned instruction patches with rollback, and the cap that a patch is never auto-approvable |
| [0022](0022-fechamento-4b-psicologo.md) | Evidence that arrives, cost that shows up, analysis that survives a kill; termination cause as a deterministic classification |
| [0023](0023-fechamento-4b-anamnese.md) | A catalog that works, dedup that distinguishes **who** declined, and a queue that survives the round |

## Phase 5 — Production

| # | decision |
|---|---|
| [0024](0024-fase5-imagens-producao-ci.md) | Multi-stage, non-root production images, `compose.prod`, CI with scan and smoke test |
| [0025](0025-fase5-deploy-kubernetes-kustomize.md) | **Kustomize** base+overlays (not Helm), engine HPA by Oban queue depth, local overlay |
| [0026](0026-fase5-observabilidade-e-graceful-shutdown.md) | Graceful shutdown with drain, session registry in `:global`, OpenTelemetry with per-session trace, metrics and dashboards as code |
| [0027](0027-fase5-backup-hardening-release.md) | Tested backup/restore, rate limiting in Postgres (no Redis), classified HTTP surface and versioned release |
| [0028](0028-protecao-de-branch-divergencia-entre-providers.md) | Branch protection diverges between GitHub and GitLab — and that's why it's **not the gate**; the gate is the domain |

## Documentation

| # | decision |
|---|---|
| [0029](0029-sincronizacao-continua-da-documentacao.md) | Continuous documentation synchronization: **generate > verify > remember**, responsibility map validated in CI, and a mandatory escape hatch |

## Branch policy

| # | decision |
|---|---|
| [0030](0030-politica-de-branches-mecanizada.md) | Mechanized branch policy: rule → mechanism, logic in a testable script, backmerge gate, and the cost of reintroducing each cut |

## Phase 7 — First-party auth

| # | decision |
|---|---|
| [0031](0031-auth-first-party-argon2id-e-rotacao-de-refresh.md) | Auth in the api's domain: argon2id with fixed parameters, EdDSA access token from a derived key, refresh rotation with family revocation on reuse, lockout keyed on email (not id), and the single invariant that closes off enumeration |

## Phase 7 — The cut

| # | decision |
|---|---|
| [0032](0032-corte-do-keycloak-e-sessao-em-cookie.md) | The atomic Keycloak cutover: our own issuer in the guard without touching RBAC, `/internal/*` outside the JWT with a shared secret, web session in an httpOnly cookie with CSRF, and a migrated user indistinguishable from an email that doesn't exist |
| [0033](0033-referencia-de-api-gerada-do-openapi.md) | The API reference comes out of the code: response DTOs that mirror the entity by type, a table test covering the summary and the real response (the one synthesized by Swagger doesn't count), and a hash manifest instead of regenerating on `--check` |
| [0034](0034-documentacao-publicada-por-degrau.md) | Documentation published by stage in a Pages subdirectory: `baseUrl` by variable, `noIndex` outside of `main` with `forceIgnoreNoIndex` in local search, and the third exception to direct pushes |
| [0035](0035-observabilidade-legivel-e-trace-sem-coletor.md) | Instrument and export kept separate (trace with a real `trace_id` with no collector), the cross-layer path via `AsyncLocalStorage` + `@Traced`, readable log in dev and one line of JSON in production, and the three correlation gaps that were dead |
| [0036](0036-telas-de-auth-fieis-ao-design-e-fontes-auto-hospedadas.md) | Auth screens faithful to the approved mock: the design system's three fonts self-hosted (the CSP blocked the CDN and typography fell back to a system font in production), the tag version baked into the artifact, contrast calculated from the tokens instead of measured by axe, and the six deliberate divergences from the mock |
| [0037](0037-cors-do-engine-e-a-porta-como-contrato.md) | The CORS the engine didn't have (`/health` returned 200 and the browser discarded it), its own plug instead of Corsica with `/internal` and `/metrics` outside the allowlist, `WEB_ORIGIN` read in a single place, and `strictPort` because the port is part of the CORS contract |

## Phase 8 — Agent hierarchy

| # | decision |
|---|---|
| [0038](0038-hierarquia-de-agentes.md) | Area, lead and delegation: an external handoff only addresses a lead or an area-less agent, consolidation into a single artifact without changing the gates' external contract, cascading budget with a sub-cap per delegation, typed failure origin retrofitted across the whole system that already classified failure informally |
| [0039](0039-actionlint-e-validacao-do-pipeline-de-ci-gerado.md) | actionlint pinned in the engine's Dockerfile (same pattern as gitleaks/hadolint), CI pipeline validation at GENERATION time instead of a new post-PR gate, and the documented gap of `.gitlab-ci.yml` having no equivalent offline static linter |

## Project panel

| # | decision |
|---|---|
| [0040](0040-moeda-do-dashboard.md) | The dashboard card and the summary line show only USD (the prices' currency of origin, with no conversion involved) — a divergence isolated to this surface, `TokenMeter` `default`/`live` remain in R$+US$; per-workspace currency preference with a manual rate is recorded and deferred |

## Phase 9 — LLM providers

| # | decision |
|---|---|
| [0041](0041-base-openai-compativel-e-contrato-de-llm-providers.md) | OpenAI-compatible base over `node:http` (not the SDK, to get an INACTIVITY timeout), error normalized with a mandatory `code` in the chunk, capabilities in two layers with agent binding refused without native tool calling, and a contract suite where the contract owns the assertions and the harness owns the dialect |
| [0042](0042-catalogo-vivo-ciclo-de-vida-do-modelo-e-preco-auditavel.md) | Catalog sync by capability, with a discovered model entering disabled and a vanished model marked instead of deleted; two independent availability axes (owner curation × remote reality); a cascade that revalidates capability when it drops a level; and price frozen in `token_usage` so old cost stays reproducible, with append-only auditing outside the outbox |
| [0043](0043-seis-providers-de-llm-e-o-fechamento-da-fase-9b.md) | The six 9b providers (NVIDIA NIM, Together, DeepInfra, Bitdeer, Vultr, OpenRouter) enter as config over the base, each investigated from scratch against the official docs — capability declared only when proven, with two real cases of a decision reverted live during implementation; the only new hook in the base is the `parseErrorFrame` that only OpenRouter (the hub) needed |

## Phase 12 — Post-dogfooding operability

| # | decision |
|---|---|
| [0044](0044-adocao-de-repositorio-existente.md) | Adoption of an existing repository with `origin` (`created`\|`adopted`) in both tables, and the PLAN as the gate: the dry-run is the `check()` from ADR 0005 without `run()`, the plan lives in the cursor (a snapshot, not a log), and nothing is changed while the decision is null — the Phase 2 runner goes out verbatim to a collaborator and is never filtered, just not called; "divergent protection" stays at the boolean level because that's all the contract exposes (ADR 0028) |
| [0045](0045-reagendamento-por-evento-do-dev-agent.md) | Event-driven dev agent rescheduling: outbox (not an in-process call) because it survives a restart between the gate's verdict and the wake; `awaiting_gate` holds the worktree per AGENT (not per task) until the gate finishes; delivery via PubSub (not Registry, node-local) with the at-most-once limit consciously accepted; the circuit breaker (RN-047) lives in the engine because `actor.id` of `backlog.task_blocked` doesn't identify an agent's sequence |
| [0046](0046-promocao-de-story-com-autoridade-do-usuario.md) | Story promotion goes back to being the user's: `proposed_ready` is a boolean and not a new enum value because the enum IS the `claimNext` gate; promoting reuses `TransitionStoryUseCase` (dead code from finding #13) and gets 12b's `task.became_claimable` for free; validation is unified in `assertPromotable` BEFORE the trigger becomes configurable, otherwise the two modes would be distinct rules with the same name; DIRECTED backfill (existing project goes to `auto`) because that behavior was already in use; the refusal is recorded before talking to the engine — the reverse of rearm, because the event asserts something about the USER, not about the engine |
| [0047](0047-operabilidade-pos-dogfooding.md) | Closing Phase 12: validation is a SCRIPT that exits != 0, not a prose script, and evidence is extracted from the database instead of transcribed; runs local and LLM-free for two verified reasons (the fork was never named; gate judgment with a local model has not been deterministic since ADR 0020), and that's declared in the first paragraph, not the footer; the Noop entered the 12b state machine — finding #10 lived on inside the very measurement instrument —, moving it to `AgentIo` instead of copying it; Phase 10's harvest was written with the gaps as `not measured` |
| [0048](0048-decisao-no-log-e-a-ordem-do-gate.md) | Two findings revisited: an action's DECISION (`created`/`approved`/`denied`) becomes a domain event with the real actor — the outbox is transport, not memory, and `events.md` had documented the three since forever without them existing; `status` in the `created` payload is what distinguishes a human click from policy auto-approval, the metric Phase 10 couldn't collect. And D5: the cause wasn't the worktree being recycled, it was `propose/3` discarding the status and the gate opening with no PR — QA swept the worktree, approved, and the task closed without a single line committed. With the order fixed, D5 dies as a consequence, and worktree-per-task (foreseen by ADR 0045) stops being necessary |

| [0049](0049-curadoria-de-modelo-por-workspace.md) | Model curation per workspace: `models.is_active` was a column for the WHOLE installation — a workspace A owner turning on a model turned it on for B too, with the spend landing in the budget of whoever didn't decide. The catalog stays global because name, price and capabilities are facts about the provider, and duplicating it per workspace would create N truths about the same model besides splitting `token_usage.model_id`; only the decision migrates, to `workspace_models`. Absence of a row IS the off state — there's no third state, and that's how RN-043 survives with no column the sync could ever run over; turning off is an UPDATE, not a DELETE, so as not to erase who decided together. The picker's route hangs off the PROJECT (the three screens that consume it already had `projectId`, none had workspace), and `isActive` leaves the `Model` entity: whoever needs it uses a type that only exists with a workspace in hand |

| [0050](0050-credencial-sempre-cifrada-verificacao-explicita.md) | Credential always encrypted and saved; verification becomes a separate action. ADR 0004's gate (test BEFORE persisting) looked prudent and produced the worst outcome: a write-only field + refusal = a user with no credential AND no text to fix, because the screen never re-shows what they pasted. Storing and verifying are different questions, and tying them together made the second depend on the first having an answer right now. Applies to LLM and git alike — same table, same promise, and treating them differently would give two screens with distinct rules for the same object. The verdict has THREE states because the tester is a NO-OP on ollama/anthropic/openai: with a binary they'd return `ok` and the screen would claim a verification nobody ran (the ADR 0041 capability rule applied here). A bad key stops being an HTTP exception and becomes a 200 result, so the two `@Catch` that mapped it went away — a filter that can never fire is dead rule |
| [0051](0051-facetas-de-capability-e-curadoria-por-uso.md) | Capability facets read from the provider, and "what it's for" as workspace curation. Sync never queried the remote for modality (`supportsVision: local?.supportsVision ?? false`), so the column was born `false` and died `false` — the 338 models from OpenRouter's first real sync all came out with no vision, including the 181 the provider declares multimodal. Image input and output become DISTINCT axes: reading a diagram and drawing one solve different problems. The other half of the request — "best for code/documentation" — doesn't exist in any catalog, and deriving it from the model's name would be a guess dressed up as data (ADR 0041); so it's the operator's opinion and lives in `workspace_models`, like all curation since ADR 0049. The two axes don't mix: marking a use doesn't turn the model on (the `is_active` column has DEFAULT `true`, and the new row is born with `false` explicit), and the screen never writes "doesn't read images", because `false` there means "the provider didn't declare it". Video was left out: zero models across nine providers, a filter that would never match anything |
| [0052](0052-dev-agent-espera-aprovacao-no-meio-do-laco.md) | The dev agent waits for approval mid-loop, instead of burning iterations. The `:pipeline` tool pending returned the string `proposed_action <id> status pending` AS THE RESULT, and the ToolLoop kept going: the agent learned nothing about the command, tried something else, and died at `toolloop.limit_reached {iteration: 8, max_iterations: 8}` without writing a single line — approvals granted arrived after the loop had run out and were useless. The terminal allowlist doesn't solve it because it's a list of anticipated commands and the model invents commands: with read commands allowed, the same agent got stuck on `git branch -a`. `:awaiting_approval` ALREADY existed (Phase 12e, for the `pr_open` at the end of the task) with all the plumbing ready — outbox event, worker, `Wake.deliver`, worktree retention —, so what's missing is applying it mid-loop. It does NOT wait by blocking: the ToolLoop runs inside the GenServer callback, and blocking there locks the mailbox that would bring the decision itself |
| [0053](0053-dev-lead-e-paralelismo-autorizado.md) | Dev Lead as an area, and parallelism authorized by the user. Today one dev goes up per module plus an extra on a one-click accept, with no session cap at all: N modules × 2 agents, and spend rises without anyone authorizing it. 14d decides that the LEAD is the one who evaluates how many agents are worth it, and that going over two in the session becomes a `proposed_action` through the same pipeline as any action with external effect. The Dev Lead didn't exist and was forbidden, along with dynamic areas via `module_map` and the generic area apparatus — the three cuts fall together because dev area members are one per `module_map` module, decided by the Architect and different in each project: there's no way to hardcode them like `qa` and `infra`. `agent_areas`/`agent_area_members` come in per project, with `max_parallel` default 2, configurable per lead; the gates' external contract doesn't change and Phase 4's suite staying green with no modification is the acceptance criterion. Anamnesis proposes raising the cap when authorization becomes routine, and never raises it on its own — automating that would be the product raising its own spend cap |
| [0054](0054-gates-como-registro-declarativo.md) | Gates as a declarative registry in `docs/gates.yml`. The flow's gates existed and had been validated by real execution, but were implicit — spread across agent prompt, pure rule, use case, test and CI —, and the Phase 10/13 lesson (metric extracted by script, never handwritten) has no way to apply to what isn't enumerable. This ADR is the top half of [0048](0048-decisao-no-log-e-a-ordem-do-gate.md): that one made an action's DECISION observable in the event log, this one makes the set ENUMERABLE. Each gate declares `evidencia: event_log | teste | ci` with the locator, because not every proof is in the log — `merge-protegida` is a cap in a pure rule that emits no event of its own (what guarantees it is a test) and `backmerge` is CI with state in `.release/gate.json`; downgrading them to `warn` would lie about the product's hardest locks. The payload filter comes along with the type because `qa-verificada` and `secops-segura` both write the SAME `pr.gate_changed`, and the same type comes out when a gate opens with no `veredito` — without the filter, opening would count as passing. The REPOSITORY gates (ADR 0030) come in too — including `backmerge` and leaving `pr-police`, `approval-ladder` and `promotion-check` out would be arbitrary, they're the same family, and `aprovacoes-da-escada` is the only CI gate with a human decision: what it measures is people approving. Enumerating already paid off twice before measuring anything — it found the missing judgment call on infra PRs, which the list had forgotten, and found that `promotion-check` is a required check with NO spec of its own |
| [0055](0055-escopo-de-caminho-na-politica-de-terminal.md) | Path scope in the terminal policy. The `terminal` approval ladder was MEASURED as unworkable: 15 turns, 209,031 input tokens against 4,546 output, 14 actions and ZERO lines written for the task, because almost every turn ended up waiting on a click. Two independent defects explain the "always asks": the agent emits `cd <path> && <verb>` and `cd` is in no allow list, and a composite command requires that ALL segments match (`decide.ts:201`), so the seeded allow is almost never reached; and "Always allow" records the WHOLE literal command, which never matches again because matching is by token prefix — the escape hatch doesn't escape. What the user is asking for, "always, as long as it's inside the project folder", is today inexpressible: only `write_file` has any notion of path, `terminal` doesn't. Execution exposed the aggravating factor: `/workspace` inside the executor IS the Brabo monorepo itself, and the hello-clean dev agent went as far as proposing to read `git_executor.ex` and `context_builder.ex` — there's no project boundary, and the reach is bigger than the project. Scope resolves by REALPATH and not by string prefix (a `..` would escape), it PERMITS without exempting (network and install still ask; being in the folder doesn't make `curl` piped to a shell safe), `deny` still wins first, and outside the scope is `require_approval`, not `deny`. It's policy, not isolation: as long as the monorepo is mounted in the container that executes, the boundary depends on the policy getting it right |
| [0056](0056-o-engine-trabalha-em-repositorio-remoto.md) | The engine works on a remote repository. `get_local_repo_path/1` returns `unsupported_provider` for anything that isn't `local`, and four consumers stop along with it — the dev agent's worktree, the terminal executor, the diff that QA and SecOps judge, and the project context. The root cause is the asymmetry: the api speaks GitHub over HTTP (created the repo, committed, created the branches) and the engine works on the filesystem, only knowing a local bare repo; a remote project does the conversational half and stops at the construction half. That's what blocks Phase 13b from existing as writes. The git plumbing is already generic — `init + remote add + fetch + checkout` doesn't care whether the origin is a path or a URL —, so what's missing is a CREDENTIAL. The engine doesn't receive the master key: it asks the api for the working remote over the `/internal/*` channel that already exists, at the moment it needs it. And the token NEVER enters `.git/config`, a decision that's a direct consequence of [0055](0055-escopo-de-caminho-na-politica-de-terminal.md): writing `https://x-access-token:TOKEN@…` into `origin` puts the secret in plain text INSIDE the folder where RN-075 just granted the dev agent auto-approved read access — a `cat .git/config` would return the token, which would travel to the LLM provider on the next turn. Scope protects against reading outward, not against a secret the product placed inside. `origin` keeps the clean URL and authentication is injected per invocation. The credential is the OWNER's (RN-058), `local` stays identical, and credential failure fails with origin `infra` |
| [0057](0057-o-gate-espera-a-aprovacao.md) | The gate agent waits for approval, the way the dev agent does. [ADR 0052](0052-dev-agent-espera-aprovacao-no-meio-do-laco.md) did this for the dev agent; the GATE agents were left out, and nobody noticed because no execution ever reached them — Phase 13b did. In the 6th real execution, `qa-automacao` ran `ls -la && find … | head -50`, a composite command with one segment outside `allow`: the `ToolLoop` correctly suspended, and `QaLeadServer` classified the suspension as an *unexpected outcome* with `failureOrigin: infra`. Two things wrong at once — the task was blocked by a decision nobody made, and the origin was a lie, against [0020](0020-destravar-gates-qa-secops.md)'s rule that origin is named and never obtained by elimination. The subagent gets a third outcome, `{:awaiting, …}`, with the whole `ctx` traveling along; the Lead, which was already a GenServer per project, subscribes to `Wake` for the SUBAGENTS (it's the actor that proposed the action that gates `task.action_settled`), keeps the in-flight state and continues the area from where it stopped. `rodar_ativas` was an `Enum.map` with no way to stop mid-way, and became recursion over the remainder — that shape is what allows suspending between two delegations without losing the first. While pending, the area doesn't consolidate, doesn't emit a verdict and doesn't block the task: it waits, and the click unblocks instead of arriving late. A denial also resumes, with the reason in the result's place. It does NOT eliminate approval, and shouldn't — the gate runs commands in the dev's worktree, and swapping the wall for a hole isn't solving anything; findings X, Z and AD remain open, and what changes is the wall becoming a queue. Declared limit: a restart mid-wait loses the loop, because `pending` lives in the lead's memory |
| [0058](0058-csp-fechado-na-api-e-escopo-de-projeto-contido.md) | CSP locked down on the api, and project scope contained at the root. Revises item 7 of [0027](0027-fase5-backup-hardening-release.md), which had decided `contentSecurityPolicy: false` because "the api serves JSON and the CSP is the web's" — true, but not the point: the alternative to a GENERIC CSP isn't no header at all, it's a SPECIFIC CSP, and for something that only serves JSON the specific one is `default-src 'none'`, which costs zero behavior. Two paths put the api's response outside the web's CSP reach: DIRECT navigation to a route (pasted link, redirect), rendered on the api's own origin, and `frame-ancestors`, which only applies to the framed document. Outside production the profile loosens only what the `/docs` Swagger UI needs, under the SAME condition that mounts Swagger — if it ever goes up in production, the CSP follows instead of silently blocking it. The options moved out of the `main.ts` literal into `security-headers.ts` for the same reason that already moved `cors-origins.ts`: at boot they weren't testable, and the proof has to be a real request, because `false` and a directives object are both "valid config" and only differ in the HTTP response. Along with it comes containing `projectId`: it arrives from the route parameter already percent-decoded by Express, so `..%2F..%2Fetc` became `../../etc` and escaped the root — hitting not just reading and WRITING `permissions.json` but also the scope that AUTHORIZES the terminal via [0055](0055-escopo-de-caminho-na-politica-de-terminal.md), i.e., the policy pointing at the wrong place. Validation lives where the root is derived, not in each caller, for the same reason the function exists: duplicated checking is checking that one day diverges. Also records the seven CodeQL dismissals with the reason for each — among them `js/insufficient-password-hash` over an OAuth `state` HMAC, where there's no password and a slow hash would be a mistake |
| [0059](0059-segredo-do-state-de-oauth-sem-default.md) | The key that signs the git OAuth `state` has no default. Born from one of [0058](0058-csp-fechado-na-api-e-escopo-de-projeto-contido.md)'s dismissals: dismissing `js/insufficient-password-hash` was right — there's no password there, the "password" is `GIT_OAUTH_STATE_SECRET`, a server-side HMAC key, and HMAC-SHA256 is the right primitive. What the argument didn't consider is that a server key **with a known default value signs nothing**, and that was the case: both read points did `process.env.GIT_OAUTH_STATE_SECRET ?? 'dev-oauth-state-secret-change-me'`. The same failure mode `WEB_ORIGIN` had already had closed in `cors-origins.ts`, and worse for two reasons — the value is PUBLIC (it's in the `.env.example` of an open-source repo, not a weak default, a published secret), and what it protects is the `state`, the only obstacle to forging the callback of a route that needs to be public: with the key known, anyone can sign `{projectId, userId, provider}` of their choice and make the callback write, into that payload's project, the git token obtained from the provider. The detail that decides the shape of the fix is that `docker-compose.prod.yml` supplied the literal as a fallback, so on the REAL error path the variable WAS defined — requiring just "not empty" wouldn't have caught anything, and that's why the example literal is rejected explicitly, along with a 16-character floor that turns the configuration table's ever-present "weak = CSRF" into an actual check. The check runs at BOOT, not on first use: an api that only failed when someone tried to connect git would stand for weeks accepting a `state` signed with a public key. Resolved into a single function for the [RN-092](../business-rules.md#rn-092) reason — there were two copies of the same literal, and diverging them would make the callback reject every legitimate `state`. Deliberate breakage: production with no variable set stops booting, which is the goal and not a side effect. The four sibling secrets of the same compose (`AUTH_JWT_SECRET`, `BRABO_SERVICE_TOKEN`, `CREDENTIALS_MASTER_KEY`, `SECRET_KEY_BASE`) were left declaredly out, in the backlog — closed later by the same rule, with no new ADR, in [RN-114](../business-rules.md#rn-114) |
| [0060](0060-superficie-de-leitura-de-codigo.md) | The Code tab's code-reading surface, contained and budgeted. 26a put `listTree` and `getPullRequestDiff` into the contract and installed the check that fails an operation with no consumer; this ADR is the consumer, and deciding to write it forced resolving three things no prior route had faced, because none received a FILE PATH from the client. **Where the search lives:** outside the contract. None of the three providers has it — GitHub and GitLab have platform-level code search with their own semantics, and `LocalGitProvider` is a bare repo —, so declaring it would either be a 13th operation with `false` capability on local (a tab that vanishes on one provider) or leaking a platform's vocabulary into the normalized contract that exists precisely to prevent that; it stays composed at the application level, over `listTree` + `getFileContent`. **How much it can spend:** three budgets (directories walked, files opened, matches returned) plus a short-TTL cache, because it's the only read whose cost grows with the repository's SIZE and not with the request's, and who pays is the OWNER's credential (RN-058/RN-082) — with no cap it's the same failure family as the dashboard's 3,824 req/min, with `429` turning into a blank screen. Breadth-first scan: cut off midway, it delivers the shallow files, which is where one usually looks. **How the path is contained:** a single function, in the [RN-092](../business-rules.md#rn-092) file, reusing the scope primitives from [0055](0055-escopo-de-caminho-na-politica-de-terminal.md) — and returning the NORMALIZED path, because returning the original would allow checking one string and sending another. The vector isn't reading the wrong file: on a remote provider the path becomes a URL segment of the API and a `..` SWITCHES ENDPOINT with the owner's token in hand. CodeQL will keep flagging it, and the price is the one already accepted in the POST-PHASE 15 — duplicating the check would silence the alert and create four copies that one day diverge. The four routes are `GET`/`role:viewer` and the controller has NO write verb: reading isn't an external effect and doesn't become a `proposed_action` (it would flood the queue with noise), and when writing arrives it's born as one — the absence of a verb is what makes this boundary verifiable instead of an intention |
| [0061](0061-tipo-da-sessao-na-criacao.md) | The session's type is creation-time data, and execution stays an event. Every session was born the same, and reaching the Creative agent meant opening it first and DISCOVERING a button in the top bar afterward — that's how the user reported the problem. The risk isn't the new field: it's that the product ALREADY knew how to tell an executing session apart, by derivation, and `findActiveExecutionSession` looks for the `active` session that carries `execution.activated` (that's what makes reactivating land on the session where the dev agents already are, finding #11 of the first dogfooding). Recording a type creates a SECOND source, and two sources diverge — here, over where the dev agents write. Deriving everything from the log doesn't work, and the reason is temporal: the intent exists BEFORE any event, in the click that opens the session, and a creative session nobody activated has no event to derive from. The decision is that the two coexist answering different questions — `kind` is creation INTENT and immutable, the event is execution STATE, and `findActiveExecutionSession` does NOT start looking at `kind` —, under the rule that keeps them from fighting: `execution.activated` on a `consultiva` session is 409, never silent conversion. The lock lives in the FUNNEL (`AppendSessionEventUseCase`), because both the user's route and the engine's `/internal/*` pass through it, and locking in the use case would leave the other open; and it runs before `incrementSeq`, so a rejected attempt doesn't consume `seq`. The field is mandatory at every creation point, including the four internal ones: optional would make the other callers silently inherit the default. The column's DEFAULT is `consultiva` (the type that can do less) and the BACKFILL is `criativa`, in the opposite direction and by the argument of [0046](0046-promocao-de-story-com-autoridade-do-usuario.md) — every existing row predates the distinction, and some are the sessions the dev agents are working in right now. Along with it come the friendly name, which is ADDED to the hashtag and never replaces it (the hashtag is what gets pasted into a URL; a person's name isn't unique), and the session screen's exit, which had none. The accepted price: the question "does this session execute?" now has two places to check |
| [0063](0063-duas-audiencias-para-o-mesmo-gasto.md) | Two audiences for the same spend: the owner's bill and the member's consumption. The request was a spend summary tab in the spirit of OpenRouter's *activity*, and the data was never the problem — `token_usage` has provider, model, actor, tokens and price FROZEN since [0042](0042-catalogo-vivo-ciclo-de-vida-do-modelo-e-preco-auditavel.md); what was missing was the AGGREGATIONS (there was none by model, by project within the workspace, by session or by person). The difficulty is the collision between two existing rules: by [RN-058](../business-rules.md#rn-058) the key the agent spends is the OWNER's, and by [RN-060](../business-rules.md#rn-060) that key's bill is his and only his — so "how much did I spend" is, literally, asking for a slice of someone else's bill. The two easy outs are both wrong: opening the credential report filtered revokes RN-060 through the back door (the answer would still talk about provider and key, and summing the lines reconstructs the bill), and showing nothing leaves the one person forbidden from seeing the consumption being exactly the one who is it. The decision separates the QUESTIONS: the owner's is by CREDENTIAL (provider, key, bill) and still requires `owner`, reused whole instead of rewritten; the member's is by ACTOR, in tokens and estimated cost, WITHOUT breaking down by provider or credential. The actor comes from the authenticated token and there's no parameter to write someone else's id into — "a member doesn't see another actor's line" becomes a property of the signature, not a check someone might forget; proven by test, and the mutation that removes the filter kills it. The provider axis DOESN'T EXIST in the new aggregation (`sumGroupedBy` has `model`/`project`/`actor`/`session`/`day` and nothing else): the absence is what keeps the member's view from getting it by accident, and for the same reason two providers serving a model with the same name fall into a single line. Agent doesn't enter the member's account because `token_usage` records who spent, not who ordered the spend. No charting library — two shapes, one series each, an inline `<rect>` handles it —, and the daily series comes DENSE from the api, otherwise three calls across three weeks turn into three sticking-together bars. No migration: measured at 525 thousand rows, the queries come out at 55 ms and 38 ms per *seq scan*, and an index on `created_at` would bring them to 32 ms and 19 ms — the gain is recorded and the index comes later |
| [0064](0064-escopo-de-area-na-cascata-e-o-binding-de-agente-global.md) | Area scope in the model cascade, and the global agent binding. The request: area leads and their subgroup sharing a current model, with the agent free to diverge — an inheritable pattern, a decision the user had already made. The cascade (`session > agent > project > workspace`) gained `area` between agent and project, and answering WHERE it goes surfaced an inconsistency that needed a decision before coding: the `agent` binding was GLOBAL (`scope_id` = plain slug, the route's `:projectId` deliberately IGNORED since Phase 9a) and `area` is PER PROJECT since ADR 0053 — a per-project scope ABOVE a global scope would make the same agent resolve different models only where an area existed, behavior dependent on a data accident. Two mutually exclusive outs: make the agent binding per-project (changes existing behavior), or put area BELOW agent (contradicts "inheritable pattern", because almost every binding today IS of an agent). The first was chosen: `agent` and `area`'s `scope_id` became COMPOSITE — `<projectId>:<slug|key>`, no new table, because neither a project UUID nor a slug/key contains `:` — and the old format became REJECTED on write, not accepted and ignored, so as not to save a binding the cascade would never find again. Migration 0040 spreads every existing global binding into one row per project (preserving what each project resolved, because the global row never held an owner) and drops the old format. The new level enters the SAME capability revalidation from ADR 0041 (RN-041/043) and also requires tool calling (RN-040) — area is only read by an agent, never by human chat. "Going back to inheriting" (RN-102) is a DELETE, never copying the model from the level below upward: copying would turn into a copy that diverges on its own the next time the area changes. Changing the area's model requires `maintainer` — the same rule as the parallelism cap (RN-083), because it reaches lead and subagents at once |
| [0065](0065-container-por-projeto-a-fronteira-deixa-de-ser-politica.md) | Container per project: the boundary stops being policy. Revises [0055](0055-escopo-de-caminho-na-politica-de-terminal.md), which had declared its own pending item: path scope is POLICY, not isolation — as long as the Brabo monorepo is mounted in the same container that executes commands, the boundary depends on the policy getting it right, and a symlink from inside pointing outward isn't detected. Findings Z and AD (verb allowlist doesn't converge — verb, form and invocation are distinct spaces) reinforce Phase 13's same conclusion: the path to autonomy doesn't go through loosening policy, it goes through changing the boundary. The user's decision, verbatim: each project gets its own container, inside it the agent is FREE (read, write, install, build, test, run), and Code only unlocks once the ARCHITECT decides which image goes up — they're the one who knows which runtime the stack calls for. The image is born as an ARTIFACT (`artifact.project_image` in the event log, versioned, no table — revising means emitting again), with an explicit OCI tag (`latest` rejected) and mandatory `rationale`; the gate (RN-105) makes the Code tab respond 409 while `sem_decisao`, in the SAME function that already contains the path (RN-092), never duplicated across the four routes. What's left as the boundary is OUTSIDE: `git push`, PR and deploy don't go through the terminal — not even inside the project's scope —, and the rule is `deny`, not `require_approval`, because "always allow" writes the pattern into `allow` and one click would reopen the door forever; the message redirects to the TYPED action (`git_push`/`git_merge`/`pr_open`), which is already the dev agent's real path. Network is decided ONCE in the artifact (`network: none` by default, `egress` authorized by the user) and not command by command — repeating the allowlist for egress would repeat the mistake Z and AD proved doesn't converge; spend (cpus/memory/pids) has a cap that REFUSES instead of silently downgrading. HONEST, declared cut: the container's lifecycle (provisioning, recycling, cleanup, what survives a restart) is left for the phase with a migration slot — container state is mutable and needs a table, and a container that comes up without recycling is worse than none. Consequence: the "inside is free" half of the terminal policy has NOT changed yet; 0055 keeps holding as-is until the wall really goes up |
| [0066](0066-nome-de-pasta-legivel-do-workspace.md) | Readable workspace folder name. Revises [0055](0055-escopo-de-caminho-na-politica-de-terminal.md): `projectScopeRoot` now receives `workspace_dir_name`, no longer the raw `projectId`, preserving the guarantee that api and engine agree on the project's root. User request: a readable physical folder (`<slug>-<8 chars of the id>`), with a unique id — `PROJECT_WORKSPACES_ROOT` is ONE root for the whole instance, and slug alone isn't globally unique. Between the two options presented (lower-risk symlink vs. actually renaming the folder), the user explicitly chose the second. The id is born in CODE (`crypto.randomUUID()` in `CreateProjectUseCase`, no longer Postgres's `defaultRandom()`), because the folder name needs it BEFORE the `INSERT`. A project created before this migration kept the physical folder it already had — the backfill writes `workspace_dir_name = id` and NEVER renames any directory, and the name is FROZEN at creation: editing the slug afterward doesn't recompute the folder. A `BEFORE INSERT` trigger (the first ever in the product's migration history) applies the same `id::text` fallback for an insert that arrives without the field — a safety net, not the main path, and what avoids rewriting more than fifty test specs that insert `projects` with no knowledge of the concept. The engine reads the SAME column (`Engine.Projects.Project.workspace_dir_name/1`) to resolve `Workspace.workspace_dir/1`, never recomputing the name — the two derivations are, in practice, the same read against the same database. Outside the tool loop's hot path (which already receives `ctx[:workspace_root]` resolved once, at the agent worktree's creation); `WorktreeCleanup` switched from scanning disk to querying `Project.all_workspace_dirs/0` in a single query, because the folder stopped being the `project_id` and there's no way back from one to the other without a query |
| [0067](0067-o-gate-sobrevive-ao-restart.md) | The gate survives a restart. [0057](0057-o-gate-espera-a-aprovacao.md) already declared "a restart mid-wait loses the loop" as a known limitation, but investigating again found a SECOND window, distinct from suspending on `{:awaiting, ...}`: `DevAgentServer.correct/3` and `Dispatcher.run_secops/2` are direct in-memory calls, made AFTER `record_gate_verdict` had already durably advanced `gate_status` on the api — a crash between the two locks the PR forever, and it's the EASIEST window to hit, not the rarest. `gate_states` (schema `engine`, key `{project_id, task_id, gate}`) records two `step`s: `in_progress` (no verdict recorded yet — no surgical resume of `ctx`, which doesn't survive a restart, but restarting the area from scratch is safe because the api rejects `record_gate_verdict` for a gate that's no longer the `gate_status` owner) and `dispatch_pending` (verdict already recorded, only the in-process call is missing). `Engine.Gates.GateRescuer` scans rows stuck for more than 15 min (generously, on purpose — QA's ToolLoop legitimately runs up to 60 iterations, and rescuing too early would duplicate a merely slow cycle), called at boot and by an Oban tick that reschedules itself, reusing the same queue mechanism the rest of the engine already uses. Two guards against duplicating work: a local `Registry.lookup` before any rescue, and the staleness threshold itself as a second line for what the first doesn't cover (a remote replica, `Registry` node-local, the same caveat as `Engine.Dev.Wake` since ADR 0045) — the worst outcome of a residual race is duplicated, cheap work, never inconsistent data |
| [0068](0068-diagrama-c4-do-arquiteto.md) | C4 diagram (Context + Container, Simon Brown) from the Architect, rendered on the Overview. `artifact.c4_diagram` with no table, same design as `artifact.project_image` (ADR 0065): the Container level is DERIVED from the current `module_map` by the use case (never retyped by the model — avoids silent divergence between the diagram and the real map), and only the Context (system name + external actors) comes from the `create_c4_diagram` tool call. `mermaid` enters as a new RUNTIME dependency of `apps/web` (user's decision) — the first of its kind; isolated behind `lib/mermaid-render.ts` with a dynamic `import()`, confirmed by `vite build` that only the entrypoint loads eagerly and Mermaid's heavy chunks (`cytoscape.esm` 435 KB, `katex` 258 KB, used only by OTHER diagram types) stay on demand. ADR 0058's locked-down CSP confirmed intact: no Mermaid chunk uses `eval`/`new Function`, and `style-src 'unsafe-inline'` already covered the `<style>` the SVG might embed |
| [0069](0069-fusao-condicional-do-handoff-com-a-ativacao-de-execucao.md) | Conditional merge of accepting the handoff with activating execution. The Dev Lead's card had two clicks because they're two deliberately distinct authorizations (RN-137): accepting requires `developer`, activating requires `maintainer` because whoever activates becomes `session.createdBy` and resolves the EFFECTIVE role of the PRs the dev agents open afterward. The request doesn't lower that bar — for someone already `maintainer`/`owner` (read from the same `useCurrentWorkspaceWithRole()` that already authorizes "Auto mode" and Approvals/Settings), the second click protected nothing, just repeated a decision the role already authorized; `handleAcceptHandoff` now chains into the SAME `handleActivateExecution` the "Activate execution" button already calls (RN-137), reusing its error handling so as not to confuse "accept failed" with "activation failed after a successful accept". Backend untouched — `RequireRole('maintainer')` stays the same, it's a decision only in `apps/web`; `developer` keeps today's flow, with the two buttons separate |
| [0070](0070-observabilidade-no-compose-local.md) | Observability in the local Compose: metrics and logs, no trace. [0026](0026-fase5-observabilidade-e-graceful-shutdown.md) set up Prometheus, Loki, Tempo, Alloy, Collector and Grafana **only for Kubernetes** — the acceptance criterion said "in Grafana local" and "local" there meant the k3d cluster. The practical consequence is that whoever develops with `pnpm dev` has the whole instrumentation running (the api exposes 102 series, the engine exposes `oban_queue_depth`, all three stamp `trace_id` in the log) and NONE of it is observable, because seeing a panel required standing up a cluster that by design doesn't coexist with `pnpm dev` (same ports, ADR 0025). The `docker/docker-compose.observability.yml` overlay is opt-in, not part of `pnpm dev`: that's four more containers on a machine already running Postgres, Ollama and three apps, and whoever isn't investigating shouldn't pay for them. It delivers the two telemetries that DON'T need a collector — metrics is scrape, logs is read from stdout, exactly the split from decision 9 of 0026 — and leaves trace out, with no `OTEL_EXPORTER_OTLP_ENDPOINT` set, because [0035](0035-observabilidade-legivel-e-trace-sem-coletor.md) already established that not exporting is the normal state in development and pointing at a nonexistent address only produces an export error on every turn. The artifacts are the SAME ones from the cluster, not a copy: the Compose mounts `deploy/k8s/observability/dashboards/` directly and the datasource UIDs are identical, because a dashboard references a datasource by UID and a dashboard copy diverges the first time one side gets a panel fixed; for the same reason the scrape emits the `app`/`pod` labels the panels group by, and the collector emits the `app` the cluster's Alloy derives from `app.kubernetes.io/name`. Log parsing is by regex and not `stage.json` because in dev `pino-pretty` draws the readable tree and the engine uses `PrettyLogFormatter` — a good decision that doesn't get swapped to please the collector; the collector adapts instead. Only `api`, `engine` and `web` go to Loki: Postgres and Ollama are noise, and the observability stack itself is left out because it runs in the SAME Compose project and would ingest its own log in a loop that feeds itself. `trace_id` is STRUCTURED metadata and never a label (cardinality), which required `allow_structured_metadata` and schema `v13` in Loki's own config. Records three modes of silent failure that cost time, all with the same signature — valid config, zero error in the log, wrong data on the panel: `stage.replace` substitutes CAPTURE GROUPS and with no parenthesis substitutes nothing; escaping `\x1b` in a River string is processed before the regex compiles, and `[[:cntrl:]]` doesn't depend on that layer; and a Go template with a missing key renders the literal `<NO VALUE>`, which turned into a level value in the selector |
| [0071](0071-publicacao-simetrica-por-degrau.md) | Symmetric publishing per stage, and a root that chooses. [0034](0034-documentacao-publicada-por-degrau.md) had each permanent branch publish its own documentation, but left the ADDRESS asymmetric: `main` at the root, `qa` and `dev` in a subdirectory. The asymmetry charged three prices — the three sites couldn't see each other (whoever landed on `/brabo/` had no way to reach `qa`), the root's special case forced a workflow step that only ran for a third of the publishes, and there was nowhere to declare maturity. The three now go to `/brabo/<branch>/`, and the root becomes a generated INDEX, not a redirect: redirecting would be simpler and would swallow the question that leads someone to look at more than one stage ("does `qa` already have what I need?"). The page offers what EXISTS in the tree, never a fixed list, and shows the stamped version read from the tags. A `404.html` at the root catches every old deep link (`/brabo/architecture` → `/brabo/main/architecture`), with a loop guard for a 404 that happens INSIDE a stage. The tree is assembled for all three (not `keep_files: true`, which never removes an orphan), which brings the migration for free: the trash `main` left at the root isn't in the assembled tree and goes away on the first push. And the transition seeds `/main/` from the old root — without it, `dev`'s first push would leave `/brabo/main/` nonexistent right while `404.html` redirects there |
| [0072](0072-projeto-local-ou-container.md) | The project chooses where the code lives: the user's folder (**Local**) or a managed folder (**Container**). Revises part of [0065](0065-container-por-projeto-a-fronteira-deixa-de-ser-politica.md) and treads on [0055](0055-escopo-de-caminho-na-politica-de-terminal.md)'s ground — both had decided the OPPOSITE direction, heading toward the container wall, and that's why the consequences section is the most important part of this ADR. User request, with the variant explicitly chosen: **freely typed path**, aware of the warning that it only works mounted in the container. `projects` gains the pair (`workspace_mode`, `workspace_path`) with a CHECK tying them together — `local` with no path would be a terminal scope pointing nowhere, `container` with a path would be a second source of truth —, and the lock lives in the DATABASE because the column is read by two processes and written by scripts that don't go through the use case. `container` is the default: an existing project doesn't change location. `projectScopeRoot` now receives the LOCATION instead of the folder name and stays the SOLE derivation — no caller gained its own validation, for the same reason the function exists. The creation guard ([RN-170](../business-rules.md#rn-170)) REJECTS with 400 **and teaches**: absolute path, no `..`, that exists, is a folder and is writable by the process (`W_OK|X_OK` — the images run non-root per [0024](0024-fase5-imagens-producao-ci.md), and a host folder owned by someone else arrives read-only); the message carries the compose line to add, because the most likely failure mode would produce a project that breaks LATER, far from the screen where the decision was made. System root, system folders, and overlap with the Brabo checkout are rejected in BOTH directions — the folder that contains the monorepo is the literal request's case, and the folder inside it is the problem 0055 reports actually happening. The lexical part of the guard also runs on READ, because the only way to bypass creation is to write straight into the database, and what's gained there is terminal scope at `/`. The Architect's image gate ([RN-105](../business-rules.md#rn-105)) stops applying in Local mode, explicitly and in the same funnel: a Local project doesn't stand up a container, and the rule as it stood would return 409 forever for a decision that will never happen. The engine resolves the locator at QUERY time (name in `container`, absolute path in `local`) and distinguishes by the leading slash, unambiguous because a folder name is validated against a slash-free regex. The consequence that hurts is declared: the STRUCTURAL containment of the `join` — no corrupted column escaping the managed root — stops existing for Local projects, and 0055's symlink vector stays open, now pointing at the user's machine |
| [0074](0074-tema-alcancavel-e-o-boot-sob-csp.md) | Reachable theme, and the boot under CSP. The light theme existed in `design/tokens.css`, was measured by a parity test and referenced in `design/README.md` — and **nothing in `apps/web` wrote `data-theme`**: the only way to see it was typing the attribute into DevTools. Both contrast tests knew this and were right while it lasted (one asserted via `expect` that three light-theme pairs FAIL, "a record of what's inherited, not a guarantee of what's rendered"); with the theme button coming, measuring only the primary theme would start hiding half the visible surface. The boot is a FILE (`public/theme-boot.js`), not an inline script: the image is served under `script-src 'self'` (ADR [0058](0058-csp-fechado-na-api-e-escopo-de-projeto-contido.md)), and inline would work in `pnpm dev:web` and get blocked in production — [0036](0036-telas-de-auth-fieis-ao-design-e-fontes-auto-hospedadas.md)'s failure with a different subject, since the handoff calls for exactly the inline version. Synchronous and before the bundle, or else a light-theme user would see a dark flash on every load; plain ES5 because vite's `public/` doesn't go through a build, locked by a test; `localStorage` that throws degrades to "paints and doesn't persist", and an unknown value falls back to the default instead of turning into a `data-theme` the CSS doesn't know. Six light-theme tokens changed value because the theme's most demanding background is paper's `--code-bg`: `--accent` 3.56 → 4.81, `--warning` 3.15 → 4.98, `--success` 3.89 → 5.12, `--violet` 4.16 → 4.95, `--text-muted` 2.76 → 5.17 (at 2.40:1 over `--surface-2` it failed even the 3:1 floor — a bug, not debt), and `--accent-hover` followed one step below. Recorded side effect: the five pairs that are known debt in DARK pass 4.5:1 in light, and the "known design-system exception" (`--on-accent` over `--accent`, 3.20:1) vanishes in light because the fix the test comment described — darkening down to `--terracota-500` — is what light now does. Dark didn't change a single value. The syntax palette becomes the handoff's EIGHT roles with the `--syntax-*` prefix, and **five of the handoff's eight values were rejected by measurement** (`--syn-cm` 2.32:1 in light, `--syn-kw` 4.34, `--syn-str` 4.20, `--syn-fn` 4.14, `--syn-op` 4.00): where the handoff fails, the measured number wins, same rule as 0036. `--font-display`/`--shadow-modal` and `--r-md`/`--r-lg`/`--r-pill` enter as ALIASES, never renames — and `--r-sm` (7px) is NOT a synonym for `--radius-sm` (4px) |
| [0073](0073-o-caminho-publicado-nomeia-o-ambiente.md) | The published path names the ENVIRONMENT, not the branch. Revises [0071](0071-publicacao-simetrica-por-degrau.md): it put the three stages at `/brabo/<branch>/` by interpolating `$GITHUB_REF_NAME`, and with that the stable documentation ended up at `/brabo/main/` — `main` is a committer's word, and a reader is choosing maturity, not branch. `main` now publishes at `/brabo/prd/`; `qa` and `dev` coincide by accident, not by rule. The branch→path map now EXISTS in one place per process (the workflow step, the `docusaurus.config.ts` `DEGRAUS` table and the one in `landing.mjs`, the latter separating the tree's directory from the branch the version TAG comes from). `E_PRODUCAO` stays `DOCS_BRANCH === 'main'` — item 4 of 0071 exists precisely for the day the path changes, and this is that day. Two traps handled: `/brabo/main/` is published and vanishes with `keep_files: false`, so `404.html` REWRITES the prefix (its own case, not the generic redirect, which would produce `/brabo/prd/main/…`) and the anti-loop guard now only covers existing paths; and the transition seeds `/prd/` from `gh-pages:main`, otherwise `/brabo/prd/` would return 404 for days between `dev`'s first push and the next promotion |
| [0075](0075-embeddings-no-contrato-de-llm-provider.md) | Embeddings in the LLMProvider contract. The Chat RAG that's coming needs vectors, and the product didn't know how to make them: the word "embedding" appeared in ONE comment, in `bitdeer-provider.ts`'s prose. The operation enters as `embed?`, optional under the same two-sided contract as `listModels` (whoever declares the capability implements it; whoever doesn't declare doesn't expose it), in BATCHES because an index receives N chunks at once — and from that comes the most important guarantee: **one vector per input or an error**, never a shorter list, because order is the only link between input and vector and a partial response is undetectable afterward. The error THROWS normalized by `code` instead of turning into a chunk, with [0041](0041-base-openai-compativel-e-contrato-de-llm-providers.md)'s taxonomy untouched: the chunk's reason is preserving spend from a turn in progress, and there's nothing to preserve here. The return states `dimensions` checked against what CAME BACK (not copied from the catalog — a vector index has a fixed dimension), the `model` the provider said it used (an alias resolves to a dated version, and that's what goes to metering for the frozen-price reason, [RN-044](../business-rules.md#rn-044)) and `inputTokens`/`estimated` with [RN-041](../business-rules.md#rn-041)'s distinction. The capability is in TWO layers as 0041 required, but with a difference that had to be named: tool calling is a gradient (a model with no tool still converses) and embedding is EXCLUSION — `nomic-embed-text` doesn't answer questions and `llama3.2` doesn't return vectors, two disjoint sets, and that's why the question didn't fit any existing `supports_*` column. Provider: `capabilities.embeddings`, MANDATORY by the same argument that made `code` mandatory. Model: `supportsEmbeddings`/`embeddingDimensions` in the catalog, optional because absence means "the provider didn't say" — Ollama is the only one of the nine that publishes this per model (`capabilities: ["embedding"]` in `/api/tags`). Only `ollama` declares `true`, proven by EXECUTION against the real 0.32.1 daemon (2 inputs → 2 vectors of 768, `prompt_eval_count: 10`), an execution that also produced the test for the `501` a chat model returns. The other eight stay `false`: seven for lack of proof (no key in the environment, and the only paid smoke test that has ever run was for CHAT — on a hub, embedding routes to a different provider), and Anthropic for lacking the operation (their docs say to use a third party). The base's dialect is proven separately, with the contract suite running a second time against it with the capability turned on, and that's what makes it cheap to turn a provider on once the key exists. Two declared cuts: embedding metering doesn't exist yet because `token_usage.session_id` is NOT NULL and indexing a repository doesn't happen inside a session, and the `models.supports_embeddings` column waited for another wave's migration slot |
| [0076](0076-provider-volta-a-ser-dimensao-de-gasto.md) | `provider` becomes a spend dimension again, and containment shifts to the TYPE. Revises [0063](0063-duas-audiencias-para-o-mesmo-gasto.md), which had excluded the axis with a no-middle-ground sentence ("the absence is structural… there's no argument to pass") — the argument is still right, and that's exactly why the reopening, decided by the product owner, comes with the containment written in. What 0063 didn't separate and usage asked for: the owner couldn't see, WITHIN the sliding window, where the money went (`credential-spend` answers by provider, but in calendar months and tied to the key that exists today), and person and agent showed up in the same ranking. `sumGroupedBy` gains `provider`, the owner's report gains `porProvider` (RN-186), and the member gains no field at all. Containment changes FORM, not strength: there are two overloads, and the scope that carries `actor` — the only one the member's view has — only accepts `SpendDimensionDoAtor`, which is `Exclude<SpendDimension, 'provider'>`; asking for `provider` with actor DOESN'T COMPILE (RN-187). No `if` on the combination, on purpose, for the same reason auto mode's resolution lives in the repository and not in `decide.ts` (RN-153/154): a dynamic check suggests a dynamic guarantee, and that's what the next refactor removes without leaving a red test. `Exclude` instead of a second hand-written list, because a parallel restricted list ages silently. Person and agent become `porOwner`/`porAgente`, a PARTITION of `porAtor` in the use case rather than two queries (RN-188) — 0063 measured that the cost grows with the size of `token_usage`, not with the request's. The index on `token_usage(created_at)` that 0063 measured and deferred enters here (migration `0044`): 55 ms → 32 ms and 38 ms → 19 ms at 525 thousand rows. Consequence declared without softening: the guarantee went from "impossible to express" to "impossible to compile", which is weaker — what holds the price down are two independent barriers, and the other is the member's route having no dimension parameter at all, with `@ts-expect-error` covering the first (if the barrier falls, `tsc` fails on the UNUSED directive, instead of the test passing while testing nothing). The SCREEN is left out, declared |
| [0077](0077-ranking-de-modelos-por-capacidade-sem-nota-inventada.md) | Model ranking by capability, with no invented score. The handoff asked for a "best models by capability" table with a SCORE per capability (code 9.4, image 9.1…) and an **ideal** badge on `ModelPicker` "when the model covers all capabilities the agent requires". The investigation found both requests need the same data the product doesn't have: the score is FICTIONAL, from the mock (no provider publishes "code quality", no product metric measures that), and "capabilities required per agent" doesn't exist in the domain — `ModelsSection` itself had already renamed the "Agent · capabilities" column to just "Agent" for that reason, and promising it again would reopen a gap that had already been refused. A second, structural barrier closes off the badge on its own: the agent-binding `ModelPicker` reads `GET /projects/:id/models` (role `viewer`), and curation (`uses`, ADR 0051) only exists in `ModelComCuradoria`, from a `maintainer`-only route — linking the two would widen RBAC, a separate product decision. The badge is NOT built (documented in `ModelPicker.tsx` itself); the ranking table IS built, with two REAL signals: "recommended"/"alternative" come from the curation (`uses`) the workspace already marked, ordered by how many agents IN THIS PROJECT already resolve to that model via the cascade (the most honest signal without inventing a score), cost as a tiebreaker; a capability with no curated coverage shows the gap, never hides the row |
| [0078](0078-moldura-de-tela-e-o-registro-de-abas-diverge-do-handoff.md) | Screen frame, and the tab registry deliberately diverges from the handoff. The handoff's "screen frame" checklist (60px header, tab bar with `box-shadow: inset 0 -2px 0 var(--accent)`, horizontal scroll, content container with a 960–1040px max width) had all four defects in `ProjectPage.tsx`, and all four were fixed — `--header-h` became `min-height`, not `height`, because the PROJECT header (identity + compact `TokenMeter`) is richer than the handoff's generic header for the 6 inner screens, and forcing 60px would cut off the budget alert, the same class of mistake RN-088 forbids. The part that needed a decision, not just CSS: the handoff lists 7 tabs and the registry (`project-tabs.ts`) has 10. `executores` (RN-121), `backlog` (RN-048) and `insights` were born AFTER the handoff, with real data and their own RN, and STAY — the handoff is a reference for VISUAL fidelity, not a ceiling on how many tabs the product has (RN-203). And the `sessions` key stays labeled "Chat", never "Chat RAG" as the more recent handoff asks (RN-202): "Chat RAG" describes a feature that doesn't exist — no indexing pipeline, no citation UI, just the contract's foundation (embeddings, [0075](0075-embeddings-no-contrato-de-llm-provider.md)) —, and renaming the tab today would announce a capability before it existed, the same mistake [0042](0042-catalogo-vivo-ciclo-de-vida-do-modelo-e-preco-auditavel.md) already refuses for catalog models. The tab bar spacing values that lived as a descendant CSS override in `ProjectPage.module.css` — a pending item declared since PHASE 16 — moved into `Tabs.module.css`, which now owns them. No export SHAPE changed (`AbaDoProjeto`, `ABAS_DO_PROJETO`); only `code`'s label went from "Code" to "Código" |
| [0079](0079-tabela-de-chunks-vetor-e-tsvector-juntos.md) | `chunks` table for the Chat RAG: `vector(768)` and `tsvector` on the SAME row (not two tables with a JOIN), an HNSW index (`vector_cosine_ops`, not IVFFlat — the table starts empty) and GIN, three honest scopes (`docs`/`adr`/`session`) mutually exclusive via CHECK. Migration `0045` creates `CREATE EXTENSION IF NOT EXISTS vector` itself, because `docker/postgres/init.sql` only runs on the volume's first initialization — and that's why it's born on a `breaking/` branch: creating the extension may require a privilege the application's role doesn't have in production |
| [0080](0080-busca-hibrida-pesos-limiar-e-citacao.md) | Hybrid search: weights, threshold and what counts as a citation. `docs`/`adr` are indexed from the project's OWN git repository (via `ReadProjectCodeUseCase`, the same credential/gate/path check as the Code tab); `session` indexes only `chat.message`/`agent.response` (the rest of the event log is mechanism, not citable knowledge). Chunking by paragraph/heading, 1200 characters with 150 of overlap — a documented starting point, not calibrated against real data. When the embedding provider (fixed: `ollama`/`nomic-embed-text`, same dimension as RN-222) doesn't respond, the pipeline saves the chunks WITHOUT a vector instead of failing outright — `search_vector` is `GENERATED ALWAYS AS` and depends on no provider at all — and declares the gap in the report, never faking full indexing. Hybrid search is TWO independent queries (`searchByVector`/`searchByLexicalQuery`, each on the index made for it), merged by a weighted sum (0.6 vector, 0.4 lexical — normalized `ts_rank` and cosine scales aren't comparable by nature) and cut at a threshold (0.2); weights and threshold are an adjustable starting point, not science. A citation is `chunkId`/`content`/separate scores (`null`, not zero, when a signal found nothing)/`origin` as a union discriminated by `kind` (`file` with `sourcePath`/`headingPath`, or `session` with `sessionId`/`eventId`). Reindexing is always MANUAL (`POST .../rag/reindex`, an idempotent full rebuild) — no watcher on push, no watcher on session close |
| [0081](0081-ciclo-de-vida-do-container-tabela-sem-orquestrador.md) | Container lifecycle: a state table, no orchestrator. Closes the cut [0065](0065-container-por-projeto-a-fronteira-deixa-de-ser-politica.md) declared about itself ("What this ADR does NOT do") — `project_containers` (migration `0046`) records mutable STATE (`status`, `image_version` pointing at the Architect's decision without duplicating it, `container_id`, DECLARED resource caps, `failure_reason`), one row per project like `dev_agent_states` in the engine ([0045](0045-reagendamento-por-evento-do-dev-agent.md)), with a pure state machine (`provisioning → running ⇄ stopped`, `failed` reachable from all three, `removed` only exits by reprovisioning) in the same shape as `session-state-machine.ts`. Prior investigation confirmed NO product service mounts `/var/run/docker.sock` or runs `privileged` — and that's why the two use cases (`Registrar`/`ObterCicloDeVidaDoContainer`) only WRITE and READ, no real Docker call: mounting the socket to "actually make it work" would be a security decision (a root-escalation vector) nobody asked for in this slice. The first transition (`provisioning`) requires the image already decided (RN-105, same gate as the Code tab) and FREEZES version/resources onto the row; a project in `workspace_mode: 'local'` ([0072](0072-projeto-local-ou-container.md)) is refused, because it doesn't stand up its own container. No new HTTP route — with no real consumer yet, exposing one would be guessing a contract |
| [0082](0082-chat-rag-aba-de-busca-hibrida.md) | Chat RAG becomes its OWN tab (`key: 'rag'`), without renaming `sessions`. [0080](0080-busca-hibrida-pesos-limiar-e-citacao.md) left the pipeline and the three routes ready; [0078](0078-moldura-de-tela-e-o-registro-de-abas-diverge-do-handoff.md)/RN-202 had deferred the "Chat RAG" label because the capability didn't exist — now it does, and the question was only WHERE it lives. `sessions` is conversation with an ACTIVATED agent (spends the owner's key per turn, RN-058); RAG is search over an already-built index, with no agent in the middle and read-only by nature — merging the two would force the same thread to express structurally different things, so the answer was a new tab (`ordem: 28`, between Code and Backlog) instead of renaming the existing one. The screen consumes the three 0080 contracts with no guessed shape (`searchRag`/`getRagCoverage`/`reindexRag`, types mirrored 1:1) and shows the two honest degradations the backend already declared and no screen was reading yet: `vectorAvailable: false` becomes a visible warning (RN-252) and coverage never invents "reindexed Xmin ago" — the response carries no such data. Citation from a `session` origin navigates to the exact EVENT reusing `useNavigate` + `highlightEvent`, the SAME mechanism the Psychologist's evidence chips already use (RN-253); a `file` origin shows path/heading as text, no link — the Code tab has no deep-link by path today, and building one is out of scope for this piece. The reindex button only shows for `owner`/`maintainer` on the client, mirroring the `maintainer` rule the route already enforces (RN-238/RN-254) — the api is still the one that guarantees it |
| [0083](0083-terminal-mostra-estado-real-do-container.md) | The Terminal tab gets the lifecycle's real consumer, not the terminal itself. Revises the part of [0081](0081-ciclo-de-vida-do-container-tabela-sem-orquestrador.md) that deferred the HTTP route until a real consumer existed — this piece (PROGRAM 28, Wave 5/F2) was that consumer. The original plan was the full interactive terminal, but Phase 25b stays cut: no service mounts `/var/run/docker.sock`, and nothing in production transitions `project_containers` (ADR 0081 created the table, not an orchestrator). Implementing a fake terminal — or one running in the same container as the Brabo monorepo, the debt 0055 already describes as policy — would invent capability, the same mistake ADRs 0041/0042 refuse for provider and model. The real delivery: `GET /projects/:projectId/container/lifecycle` (RN-267), a faithful mirror of `ObterCicloDeVidaDoContainerUseCase`, and `CodeBottomPanel.tsx` showing that state (translated badge, failure reason) UNDER the explanatory text that already existed — never replacing it —, fetched only while the Terminal tab is open (RN-268). `null` (never provisioned) is today's honest, most common result |
| [0084](0084-login-social-github-e-gitlab.md) | Social login (GitHub/GitLab), revising the backlog from ADRs [0031](0031-auth-first-party-argon2id-e-rotacao-de-refresh.md)/[0032](0032-corte-do-keycloak-e-sessao-em-cookie.md) — the ban lifted only for this piece, by the product owner's explicit decision. `SocialLoginCallbackUseCase` ends by calling the SAME `EmitirSessaoUseCase` as password login — no second session format. The OAuth client is reused (`GithubOauthClient`/`GitlabOauthClient` gain `buildLoginAuthorizeUrl`/`fetchIdentity`), but the `state` is NOT: `domain/auth/social-oauth-state.ts` signs with the SAME `GIT_OAUTH_STATE_SECRET` key only because the payload is structurally incompatible — a `purpose: 'social_login'` discriminant checked BEFORE any other field is what keeps a `state` from git's CONNECTION flow from being accepted here (RN-273), which would be privilege escalation. New `social_identities` table (migration `0047`), keyed by `(provider, provider_user_id)` — never email/login, which can change owner (RN-276). The callback decides in three steps: identity already known → log in; email matches an existing account AND is provider-verified → link, and while at it verify the account's email if it wasn't already (RN-279); email matches but NOT verified → refuse, account takeover (RN-274); no matching account → provision a NEW passwordless user, reusing the same "pending" state from the Keycloak migration (RN-278) — here the email doesn't need to be verified, because there's no account to protect (RN-275). Reuses the SAME OAuth app as the git connection flow — zero new environment variables —, changing only `redirect_uri` and `scope` (minimal, no `repo`/`api`) per request; the OPERATOR action of registering the second callback with the provider is what justifies the `breaking/` branch. The access token never travels in the URL: session cookies are written in the callback, and the web boot already rebuilds the session on its own |
| [0085](0085-fluxo-como-registro-declarativo.md) | `docs/fluxo.yml` records the RELATIONSHIPS between agent-team roles — who delivers what to whom — the third piece alongside the catalog (`agent-areas.ts`, PHASE 18) and the control (`docs/gates.yml`, ADR 0054), which until now only existed implicitly in prompts and use cases. Contracts per role (mission, typed inputs/outputs, an exit gate referencing `gates.yml`) with status `active`/`planned`/`proposto`/`em-refinamento`; a `proposto` role DECLARES who absorbs it today and the objective criterion for separation, so the target org chart becomes an activation sequence rather than an aspiration. `docs/.docmap.yml` gains the `modelo-de-time` rule at `warn` — the fluxo.yml × gates.yml × agent-areas.ts cross-check test is missing to promote it to `block` (backlog) |
| [0086](0086-dev-lead-plano-suspende-para-aprovacao.md) | The Dev Lead's execution plan becomes a real `proposed_action`, revising part of [0053](0053-dev-lead-e-paralelismo-autorizado.md) — the fluxo.yml × code audit found the flow already declared `via: proposed_action` for this output and the code was never adjusted to match. Direct precedent: [0052](0052-dev-agent-espera-aprovacao-no-meio-do-laco.md) and [0057](0057-o-gate-espera-a-aprovacao.md), but both are triggered by `cast` and never waited on a synchronous reply — the Dev Lead is the FIRST conversational agent (`GenServer.call` of up to 180s) to suspend. `TurnoAssincrono.tratar_resultado/2` gains a branch: `state` carrying the `:aguardando_aprovacao` key (non-nil value, not just present — the Dev Lead carries it since `init/1`) replies to the `from` the same way, at the same time, but emits only `agent.status: awaiting_approval` instead of `agent.done`. `LiveBroadcast.agent_status/4` widened its guard to accept the new status. `DevLeadServer` subscribes to `Engine.Dev.Wake` under its own id (the same module `QaLeadServer` already reuses for QA subagents) and resumes via `{:action_settled, ...}`; a second `user_message` during the wait is refused with `agent.error` (origin `politica`), no new turn. `propose_execution_plan` was CONSIDERED and left out of `decide.ts`'s absolute-cap block — unlike `parallelize`/`raise_max_parallel`, it can be configured to auto-approve. Accepted, declared gap: with no state table, a restart during the suspension loses the `Wake` subscription — the decision stays recorded and visible in Approvals, but the Dev Lead doesn't narrate the outcome on its own |
| [0087](0087-ux-designer-agente.md) | The UX Designer enters as the fifth conversational agent (Creative, PO, Architect, Dev Lead and now this one), SOLO with no area, ANTICIPATING the separation trigger `docs/fluxo.yml` (ADR 0085) had always declared for the role ("when the MANAGED project has its own interface to design") — a conscious decision by the product owner, not a misreading of the criterion. `UxDesignerServer` mirrors `DevLeadServer` (cap 14, bounded loop); the kickoff reads the Creative agent's `artifact.product_brief`, and the design system (`design/tokens.css`/`COMPONENTS.md`) is DESCRIBED in the identity — conversational agents have no repository-reading tool. The single tool, `propose_prototype`, writes `artifact.prototipo_navegavel` with NO dedicated api use case (unlike `project_image`/`c4_diagram`, which need one because their content is DERIVED or shares a domain refusal — neither applies here): shape validation lives in the engine (`ArtifactSchemas`) and writing goes through the generic `append_event_returning`. A single artifact, two handoffs over it (to "po" and to "dev-lead") — never a second artifact for "spec-visual", which IS the same prototype. Reuses the half of ADR 0086's design that survives without its suspension: success ends the turn, so as not to duplicate proposals |
| [0088](0088-staff-agente-dormente-para-disparo-automatico.md) | The Staff (`docs/fluxo.yml`, `camada_decisao_tecnica`, `status: planned` since [0085](0085-fluxo-como-registro-declarativo.md)) gets CODE before its automatic trigger exists — a conscious decision by the product owner, knowing Anamnesis (which would fire the handoff on "recurring systemic problem") is paused (`ANAMNESE_ENABLED=false`). Sixth solo conversational agent, mirroring the Architect (per-session GenServer, bounded loop, cap 14), with two differences: NO `kickoff/1` (there's no session artifact to summarize — it stays idle until the first `user_message`) and activation via the GENERIC `canActivateAgent` path (a handoff `accepted` addressed to "staff"), without entering `USER_STARTED_AGENTS` — investigation confirmed no domain change was needed in the api, because `assertHandoffTargetAllowed` only refuses an area subagent, and Staff has no area. The single tool, `propose_rfc` (problem, options with trade-offs, recommendation, throwaway PoC), writes `artifact.rfc_staff` DIRECTLY via `append_event_returning` — the same tableless pattern as `emit_insight`, not the one for `artifact.c4_diagram` ([0068](0068-diagrama-c4-do-arquiteto.md)), because the RFC derives nothing from the api side — and returns the handoff to the Architect in the SAME tool call, no `proposed_action` (recording a document isn't an external effect). Declared, not hidden: `SessionPage.tsx`/`AGENTES_DE_CHAT` were untouched — the same pattern already accepted for `infra`, a real lead that's also left out of that list |
| [0089](0089-analytics-e-delivery-metricas-como-relatorio.md) | `analytics` and `delivery-metricas` (`docs/fluxo.yml`, `status: proposto`) become `status: active` as a report SCRIPT — the product owner's decision to bring forward both roles without waiting for the organic trigger the flow itself already declared. `apps/api/scripts/analise-funil.ts` is the only new artifact, in the SAME shape as `medir-execucao.ts` (Phase 13b): pure Drizzle reads, zero writes, no GenServer, no LLM agent — the shape `docs/fluxo.yml` already prescribed ("never becomes an agent" for `delivery-metricas`). Measures a real funnel (session → commit → PR → merge, counting SESSIONS per step, only `executed` actions), real lead time (first commit to first merge, by the execution's `updated_at`, not the proposal's) and real deployment frequency (merges to a `PROTECTED_BRANCHES` branch, cross-referenced with the `backmerge` gate). Three metrics stay DECLARED absent, on purpose: the full product funnel from ideation → commit (`sessions` has no `storyId`, RN-230), evidence of adoption per feature (Brabo doesn't instrument what it builds — no path to that today, not just no data), and MTTR/change failure rate (they depend on a real incident signal, the same dependency as `secops-runtime`/`platform`) |
| [0090](0090-qa-estrategia-e-appsec-segundo-momento.md) | Two "second moments" of the same conceptual design, each decided by the product owner to bring forward a trigger `docs/fluxo.yml` had already foreseen. The `implementavel` gate moves from `planned` to `active`: QA-strategy stops being a `proposto` role and becomes the SAME `qa-lead`, in a second MOMENT, same process (`Engine.Gates.QaLeadServer.run_design/3`, additive to the usual `run/2`), a separate deliverable — `Engine.Gates.QaEstrategiaAgent` (stateless, no `Terminal`, LIGHT context reusing `list_backlog`/`get_infra_context` with no new route) produces `artifact.plano_de_teste` PRE-DEV. The Dev Lead gains `assess_implementability`: with no plan yet, it triggers the assessment and returns an error asking for a retry (RN-163); with a plan, it proposes the verdict as a `proposed_action`, the SAME pattern as `propose_execution_plan` ([0086](0086-dev-lead-plano-suspende-para-aprovacao.md)). `appsec` (RN-360/361) follows the SAME pattern over secops: `Engine.Gates.SecOpsAgentServer.run_design/2`, in the same process as PR secops, produces `artifact.threat_model` (a STRIDE-lite checklist over the story's DESIGN) and creates a handoff to architect, dev-lead and infra. Neither agent EVER suspends — no tool goes through `ActionPipeline` — and the iteration cap stays at 8 (conversational) on purpose, the same reason as `infra-workflows`: with no `token_budget_micros` underneath, raising the cap would multiply the worst case with nothing to contain it. Both are ACTIONABLE, but the automatic trigger (`assess_implementability` calling appsec's `run_design`) still doesn't exist — out of scope for this delivery (RN-340/341/360/361) |
| [0091](0091-secops-runtime-relatorio-de-abuso.md) | `secops-runtime` (`docs/fluxo.yml`, `proposto` role of the security layer) brought forward as a report SCRIPT, neither an LLM agent nor a `GenServer` — a conscious decision by the product owner, even with the declared trigger ("production with real traffic, post `DEPLOY_ENABLED` + `platform` active") still not having fired. `pnpm --filter api relatorio:seguranca-runtime` reads `rate_limit_hits` (the data ADR 0027's `RateLimitGuard` already records, including under dev/CI traffic) and produces a bucket ranking (`user:<uuid>`/`ip:<address>`, the only two formats ever recorded — no route or reason, which the table never stored) and a time distribution in fixed slices. Automatic incident detection, incident response and postmortem stay OUT — they depend on the same trigger that hasn't fired — and the report lists them in a permanent "not measured" section, with no example incident simulated and no invented number (same principle as ADRs 0041/0042/0077). The retained window is short (`DomainGaugesCollector.pruneRateLimit` erases hits older than `2 × RATE_LIMIT_WINDOW_MS`, 240s by default) and the report declares both windows — CONFIGURED and OBSERVED — never letting the second pass for a longer history than it is |
| [0092](0092-platform-relatorio-de-telemetria-sob-demanda.md) | `platform` (`docs/fluxo.yml`, `status: planned`, activation tied to `DEPLOY_ENABLED` — which doesn't exist) is born as an on-demand report SCRIPT, neither an LLM agent nor a `GenServer`: `pnpm --filter api relatorio:telemetria [--projeto <uuid>]` reads the SAME questions `DomainGaugesCollector` already collects every `METRICS_GAUGE_INTERVAL_MS` for the Prometheus scrape — active/closing sessions per project, blocked tasks per project, the last backup's state (always GLOBAL, never per project) — but as a ONE-OFF, standalone read that ends after printing. The SQL queries are replicated, not imported from the collector: its methods are private and end up writing to a Prometheus gauge, with no pure "just the query" half to reuse without coupling a CLI script to an `@Injectable`'s lifecycle. The output always links to what already exists, versioned (dashboards, alerts, runbook, `pnpm dev:obs`) instead of duplicating it, and declares — never invents — what it does NOT measure: a formal numeric SLO (none defined), postmortem (no real incident) and closed-loop telemetry back into the product (that's what would make `platform` `active`, and the trigger is still absent). The role's `status: planned` and `gate_saida: operavel` don't change; `docs/fluxo.yml` just gains a `nota` on the `telemetria-consolidada` output saying the manual version is already real |
| [0093](0093-dbre-linter-de-migracao-e-relatorio-de-backup.md) | The `dbre` role (`docs/fluxo.yml`, ADR 0085) becomes two mechanical scripts, brought forward by the product owner's decision without waiting for the trigger ("real data volume") — neither an LLM agent (there's no natural-language judgment to make) nor a `GenServer` (there's no long-lived loop or state), because neither deliverable needs that: `lint-migracao.ts` is STATIC analysis of SQL text (`DROP TABLE`/`TRUNCATE`/`DROP COLUMN`/`ALTER COLUMN ... TYPE`/`ADD COLUMN ... NOT NULL` with no `DEFAULT`, pure logic separate from the I/O adapter, same design as `pr-police.ts`) and `relatorio-backup.ts` just READS `backup_runs` with the same logic as `DomainGaugesCollector.collectBackup()`, formatted as an on-demand report, citing the already-tested restore (`docs/runbook.md#restore`) instead of re-running it. The linter scans the WHOLE repository (not a PR's diff) and for that reason did NOT go into CI — running it today finds three occurrences in migrations already merged and accepted (0006, 0007, 0034), and a gate that fails the whole repository would fail every PR forever; turning it into a real gate requires scoping it to the diff, as `pr-police.ts` already does, for when `dbre` needs to BLOCK and not just opine. `plano-de-capacidade`/`tuning` stay a declared GAP: they depend on real data volume, and simulating one without load would be inventing a number (same rule as 0042/0077) |
| [0094](0094-delegacao-dev-lead-vira-dado.md) | The Dev Lead → `dev-<module>` delegation becomes DATA in `delegations` (`area: 'dev'`), closing finding B1 of the fluxo.yml × code audit and revoking the cut declared in [0053](0053-dev-lead-e-paralelismo-autorizado.md) item 5. Written from the API side, inside `AcceptParallelizationUseCase.execute` (covers both paths — direct and approved — for free, because both already converge there), unlike QA/Infra which write from the engine side because that's where the subagent produces a verdict. `status: 'completed'` is REDEFINED for this area: it means "the delegation was EFFECTED" (the agent came up), not "verdict issued" — and `parecerArtifactId` points at the `id` of the project's most recent `artifact.module_map` event (via `SessionEventRepository.listByTypeForProject`, an existing method, reused without generalizing), the artifact that justified the delegation decision. With no module_map in the project: it does NOT write a fake id, it just logs the unexpected state (RN-059) and moves on — and a `RecordDelegationUseCase` failure never brings down the activation, which is already a success by the time the write is attempted |
| [0095](0095-gate-necessidade-validada.md) | The `necessidade-validada` gate (`docs/fluxo.yml`, `criativo` role, `proposto` since ADR 0085) closes the LAST wave of the fluxo.yml × code audit (finding B2), with a SEPARATE human confirmation — never the Creative agent self-validating, the anti-pattern already recorded in `modelo-de-time.md`. A dedicated button in `SessionPage.tsx` (same pattern as "Confirm architecture ready", RN-160), route `POST .../agents/criativo/validate-necessity`, `ValidateNecessityUseCase` writing `necessity.validated` — enabled only AFTER `confirm_readiness` has already consolidated `artifact.product_brief` (can't validate what doesn't exist yet), and it doesn't signal the engine: the Creative→PO handoff already happened inside `confirm_readiness` itself, so this event is just the record of merit, not an activation trigger. `docs/gates.yml` gains the gate as `active`/`warn` (nothing today CHECKS its passage before letting the PO move on) |
| [0096](0096-smtp-real-no-mailsender.md) | Real SMTP in `MailSender`, closing the backlog item open since [0032](0032-corte-do-keycloak-e-sessao-em-cookie.md) ("real SMTP stays future config"). `nodemailer` over plain SMTP transport — a stateful LINE protocol with MIME/STARTTLS/AUTH, unlike the JSON HTTP APIs the rest of the product integrates — behind `MAIL_TRANSPORT` (`log`, default including in production, or `smtp`), selected by a `useFactory` in `AuthUseCasesModule` without touching any use case. `SMTP_HOST`/`SMTP_USER`/`SMTP_PASSWORD`/`SMTP_FROM` follow the RN-114 pattern in production when the mode is `smtp`, but WITHOUT the public default the four original RN-114 variables had — the bar only kicks in once the operator opts into `smtp`, validated in `SmtpMailSender`'s constructor (exercised by the `useFactory` when the provider graph is assembled, same design as `CREDENTIALS_MASTER_KEY`), not by an eager call in `main.ts`. Plain-text body, never HTML — the port carries no structure for rich bodies. Investigation found a real gap before implementing: `email_verification` had no web route at all (`verificarEmail` existed in `apps/web/src/lib/auth.ts` with no caller) — `/verificar-email` (`VerifyEmailPage.tsx`) closes that, mirroring `SetPasswordPage.tsx` |
| [0097](0097-leitura-direta-do-schema-do-engine-para-online-agent-count.md) | The dashboard's "N agents online" (RN-409) reads `engine.dev_agent_states` DIRECTLY — raw SQL batched per workspace, inside the SAME `Promise.all` that already sums eleven queries of RN-090's read model —, instead of exposing a new internal HTTP route on the engine. `api` and `engine` share the SAME physical database, split only by Postgres schema (`"engine"` vs `public`), and there's already precedent for cross-schema reads (`apps/api/scripts/medir-execucao.ts` already reads `engine.oban_peers`) — this decision raises the bar from a manual script to TESTED production code, with a minimal, declared fixture of the engine table in `test/support/global-setup.ts`. Accepted consequence: the query assumes whoever operates the product migrates both sides together (`db:migrate` AND `engine:migrate`), and fails loudly/visibly if only the api migrated — no try/catch hiding the error |
| [0098](0098-limites-de-transporte-e-janela-efetiva-de-compactacao.md) | The `413 request entity too large` on PRs had its cause inside Brabo's own api, never the LLM provider (comments blaming the provider were wrong). Two ends close together (RN-412): `apps/api/src/main.ts` never configured Express's body limit (the 100 KB default applied, against the 8 MB Phoenix accepts on the heaviest engine→api leg, `POST .../llm-turn`) — it gained `API_JSON_BODY_LIMIT` (default 10 MB); and `Engine.Harness.ContextManager.Default` had compaction structurally unreachable before the overflow — `estimate/1` didn't count `toolCalls` (they cost ~zero tokens) and the window used only `context_window` (128k, ~350 KB before compacting). The EFFECTIVE window becomes `min(context_window, transport_cap)`, with the cut always at a `ToolLoop` iteration boundary so as to never separate an `assistant` with `toolCalls` from the `tool` messages that answer it |
| [0099](0099-neo4j-grafo-de-conhecimento-e-templates.md) | Neo4j comes in as a KNOWLEDGE graph — memory DERIVED from the event log, never a source of truth — for versioned prompt templates (`PromptTemplate`/`PromptVersion`) and relational memory (interactions, Psychologist hypotheses with evidence, Anamnesis profiles, handoffs). pgvector STAYS the chunks' vector index — the graph stores no embedding. The `neo4j-driver` lives in the API (not the engine): the API already owns all persistence and the existing RAG, the engine consumes it over internal HTTP with a service token as always. `GraphStore.onModuleInit` never throws — the three env vars are mandatory in production and optional outside it, unavailability becomes a `GraphUnavailableError`/503, never a raw 500. A pattern explicitly inspired by the [ErickWendel/neo4j-ai-experiments](https://github.com/ErickWendel/neo4j-ai-experiments) repository (graph as agent memory + prompts as files separate from code) |
| [0100](0100-rag-search-e-modelos-garantidos-no-boot.md) | New `rag_search` tool for the engine's agents — closes the biggest gap in the existing RAG (pgvector, hybrid search): no agent had ever queried it, only the "Chat RAG" web tab. Calls `POST /internal/rag/search` (reuses `HybridSearchUseCase` with no duplicated logic), formats hits with citation (`path` + snippet), degradation (`degraded: true`) always visible at the START of the text. Its own caps (RN-150): `top_k` clamped to 10, a 16 KiB byte cap. A new one-shot `ollama-model-loader` service (dev and prod) closes a separate real bug: `nomic-embed-text` was never pulled automatically, and RAG silently degraded to lexical-only in any clean environment |
| [0101](0101-memoria-relacional-como-projecao-do-event-log.md) | Consuming the graph foundation: `InstructionFiles` gains a `:graph` source (precedence `db > graph > dir > root`, reusing the existing ETS cache), with no change to any GenServer's contract — ux-designer, Psychologist and Anamnesis now TRY resolving a template from the graph before the inline text, which becomes the fallback. Two separate flags (`graph_templates_enabled?`, `graph_instruction_templates_enabled?`) on purpose — a single key would clash with OPPOSITE defaults between parallel workstreams. Psychologist/Anamnesis compose "recent"/"time window" with RELEVANT snippets via `rag_search` (never a replacement, always additive — with no hit, behavior is identical to before). The graph writes itself: `GraphProjector` drains a SECOND outbox lane (`aggregateType: 'graph_projection'`, never matched by the engine's filter) and calls the already-existing write use cases — the engine NEVER writes to the graph directly, preserving the event log as the single source of truth |
| [0102](0102-revisao-do-adr-0065-teto-absoluto-substitui-deny.md) | Revision of [ADR 0065](0065-container-por-projeto-a-fronteira-deixa-de-ser-politica.md), a GLOBAL decision by the product owner (explicitly confirmed after an automatic security warning): `git push`/PR/deploy (RN-106) and `sudo`/`doas` (new) move from unconditional `deny` to an ABSOLUTE CAP — unconditional `require_approval`, the same code pattern as the other caps, never auto-approvable by auto mode nor by `permissions.json`. The "always allow" loophole was closed AT THE SOURCE (`ApproveAlwaysActionUseCase` refuses to write a pattern for these commands) — without that, the cap would be decorative |
| [0103](0103-runner-local-execucao-na-maquina-do-usuario.md) | `apps/runner` (new workspace, Node/TS): a CLI that runs on the user's machine, connects to the engine over a new Phoenix channel (`/runner`, single-use ticket ISSUED BY THE ENGINE) to run an ALREADY-APPROVED agent command in the user's `$SHELL`, plus an interactive PTY terminal for the Code tab (the engine does pure byte relay). Routing always happens AFTER approval — never a second execution path. With no runner connected, it falls back to the usual `System.cmd` (ADR 0072 becomes the fallback). The security boundary is NOT sandboxing: it's authentication + the approval pipeline + the user's consent — `guard.ts` is declared best-effort. Real finding: with no long-lived account token mechanism in the product, the runner replicates the browser login until a real automation token exists |
| [0104](0104-execution-mode-tres-valores-e-workspace-verificado-pelo-runner.md) | ADRs 0072 and 0103 had never talked to each other: RN-170 requires a bind-mount at creation, RN-420 routes to the runner under the SAME `workspace_mode == 'local'` flag with no bind-mount at all — actually using the runner still had to pass the mounted-folder validation. `workspace_mode` (2 values) becomes `execution_mode` (3: `container`, `mounted` — the old `local`, RN-170 now conditional on it — and `runner`, with no bind-mount). A `runner` project is born with a DECLARED path and `workspace: unverified` state, promoted to `verified` only when a runner connects and confirms it on the HOST — the same spirit as the evidence-by-event gates `docs/gates.yml` already uses. Converting between the three values is now allowed, with no need to recreate the project. Product owner's prioritized backlog, recorded in this order: distributing the runner via `npm publish` (`@brabo/runner`, `tsup`), a long-lived account token (PAT) replacing the login replay in `apps/runner/src/auth.ts` — BEFORE distribution —, exclusivity by `{project_id, machine_id}` DEFERRED until a second real simultaneous dev, and `guard.ts` REAFFIRMED as best-effort — the real security boundary is authentication + approval + consent, never sandboxing |
| [0105](0105-personal-access-token-do-runner-escopado-por-construcao.md) | The Personal Access Token (`brb_…`) closes the backlog item from ADR 0104: `apps/runner/src/auth.ts` swaps replicated login (password+cookie on disk) for a long-lived, project-scoped token. New `personal_access_tokens` table, HMAC-SHA256+pepper hash via `hashDeToken()` (a fix to the backlog, which had said argon2 — wrong for a HIGH-entropy secret). Central decision: the PAT never authenticates outside `POST .../runner-ticket`, BY CONSTRUCTION — `IS_PAT_ROUTE_KEY`/`@RequirePatAuth()` (a third branch in `JwtAuthGuard`, same pattern as `IS_SERVICE_ROUTE_KEY`) + a route-level `PatAuthGuard`, never a branch in the global guard that would let `RolesGuard` authorize the PAT for any route under the user's role. `last_used_at` is an unconditional `UPDATE` in the same validation query — no throttling, which would reject a legitimate reconnection under 5 minutes with a 401 |
| [0106](0106-distribuicao-do-runner-via-tsup-e-npm-publish.md) | Closes the last ADR 0104 backlog item: `tsup` bundles `apps/runner` into a single `dist/index.cjs` (`node-pty` as a mandatory `external` — a native binding, can't be bundled), published as `@brabo/runner` by its OWN workflow, triggered by the same final tag as `release.yml`. Real, empirically-tested finding: the obvious fix for the auto-run guard (`import.meta.url === pathToFileURL(argv[1]).href`) was BROKEN in the exact case this piece exists to enable — `process.argv[1]` is never realpath-resolved, `import.meta.url` always is, and `npm install -g` creates exactly that asymmetry via a symlink; the final fix applies `realpathSync` to `argv[1]` before comparing. The version is injected only into CI's disposable checkout (`npm pkg set`), never committed. Mitigates npm's `latest` regressing when republishing one of the six orphan tags already in the repo — it publishes under its own dist-tag when the target version is older than the current `latest`. `breaking/` — an operator action (creating the npm Automation Token) is required before publishing works end to end |
| [0107](0107-navegacao-de-pasta-local-via-o-runner.md) | Local folder navigation, revising (without editing) ADR 0072: the folder picker 0072 had refused because it required the api to enumerate the CONTAINER's filesystem now exists via the RUNNER (ADR 0103), which already has the right view — the user's real machine. Two new events on the SAME `terminal:<projectId>` channel (`fs_list_dir`/`fs_home_dir`), same pure-relay design as the PTY, correlated by `ref`. The api still enumerates no filesystem at all — no new routes. The security boundary stays the one from 0103 (authentication + no-effect reads + consent), not a path allowlist — `guard.ts` (which restricts an approved command's `cwd`) deliberately does NOT apply here, because the point is FREE browsing. Declared gap: on the project CREATION screen the ticket is per-project and the project is only born on confirmation, so "Browse folder..." there shows the declared state instead of faking an impossible connection — it works end to end where the project already exists |
| [0108](0108-projeto-runner-nasce-ao-navegar-pasta-no-wizard.md) | Closes the gap ADR 0107 itself had declared: in `runner` mode, `NewProjectWizard.tsx` now creates the project when "Browse folder..." is clicked, not only on confirmation — using the path already typed, or a lexically valid, provisional placeholder (`/workspace-a-confirmar`, overwritten once the real runner connects, RN-423) when the field is still empty. Reused by an IDENTITY snapshot (`name`/`externalId`/`adotando` — never the path, whose refinement is the very point of browsing): unchanged identity reuses the same project, changed identity creates another. `mounted` mode doesn't change — path validation touches disk at creation, so it still waits for confirmation. Accepted side effect: an orphaned "unprovisioned" project if the wizard is closed before finishing, the SAME state any interrupted creation already produces today |

## The convention

- **One file per decision**, at `docs/adr/NNNN-short-title.md`, with
  sequential 4-digit numbering. **No number reuse**, not even when an ADR
  is superseded — the next one is **0109**.
- **Three sections, only those:** **Context** (the problem or force that motivated it),
  **Decision** (what was decided), **Consequences** (the accepted trade-offs and
  what's left for later).
- **No third-party template** — no Nygard, MADR or variants. The three
  sections are enough.
- **In Portuguese**, like the rest of the project.
- **An accepted ADR is not edited.** One superseded by a later decision stays as
  it is; the new ADR references the old one and explains what changed.

What gives these ADRs their value is recording what did **not** work: 0020
documents nine failed executions in a row, and 0027 records a justification
that was revised within the same session that produced it. An omitted bad
consequence is the part that's missed six months later.
