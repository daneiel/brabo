{
  "projectId": "368b0431-c98f-4b29-9cb3-b633c058e3df",
  "shape": "package",
  "pkg": "web",
  "globalName": "BraboDS",
  "srcDir": "src/components",
  "tsconfig": "tsconfig.app.json",
  "guidelinesGlob": [
    "../../design/*.md"
  ],
  "extraEntries": [
    "../../.design-sync/styles-entry.ts",
    "../../.design-sync/preview-surface.tsx"
  ],
  "provider": {
    "component": "BraboSurface"
  },
  "componentSrcMap": {
    "BraboSurface": null
  },
  "dtsPropsFor": {
    "AgentBoxIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "AlertIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "ArrowUpIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "BellIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "BranchIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "BrandIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "BulbIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "ChatIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "CheckIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "ChevronDownIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "ChevronRightIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "ClockIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "CodeIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "CommitIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "DeployIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "DiffIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "GaugeIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "GitHubIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "GitLabIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "HypothesisIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "LayoutSidebarIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "LocalRepoIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "LockIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "ModelIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "PermissionIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "PlusIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "PrIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "SearchIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "ServerIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "SessionIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "SettingsIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "StackIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "StopSquareIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "TerminalIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "ToastProvider": "  /** A árvore que passa a poder chamar `useToast()`. */\n  children: React.ReactNode;",
    "TrashIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "UserIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "XIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "AlertCircleIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "EyeIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "EyeOffIcon": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "LogoMark": "  /** Lado em px — o ícone é sempre 1:1 sobre um grid de 24. Default 16. */\n  size?: number;\n  /** O traço é `currentColor`: a cor vem do contexto (ou desta prop). */\n  color?: string;\n  className?: string;\n  style?: React.CSSProperties;\n  /** Default 1.6 — mexer nisto quebra a consistência óptica do set. */\n  strokeWidth?: number;\n  /** Ícone decorativo ao lado de um rótulo: marque como escondido. */\n  'aria-hidden'?: boolean | 'true' | 'false';\n  /** Ícone sozinho e significativo: dê a ele um nome acessível. */\n  'aria-label'?: string;\n  onClick?: (event: React.MouseEvent<SVGSVGElement>) => void;",
    "ProjectCardSkeleton": "  /** Não recebe nada: é o esqueleto de UM card, com as mesmas medidas do\n   * ProjectCard real para a lista não pular quando os dados chegam. */\n  children?: never;",
    "ModelCatalogSection": "  /** Workspace cuja CURADORIA está sendo editada (ADR 0049). O catálogo é\n   * global; o que pende do workspace é a decisão \"aparece no seletor?\". */\n  workspaceId: string;"
  },
  "overrides": {
    "Modal": {
      "cardMode": "single",
      "primaryStory": "Confirmacao"
    },
    "ToastProvider": {
      "cardMode": "single",
      "primaryStory": "OsQuatroTons"
    },
    "ProjectCard": {
      "cardMode": "column"
    },
    "Tabs": {
      "cardMode": "column"
    },
    "ModelPicker": {
      "cardMode": "single",
      "primaryStory": "CatalogoAberto"
    },
    "Table": {
      "cardMode": "column"
    }
  },
  "docsDir": "../../.design-sync/docs",
  "readmeHeader": ".design-sync/conventions.md"
}
