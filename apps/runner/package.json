{
  "name": "@brabo/runner",
  "version": "0.1.0",
  "license": "MIT",
  "description": "CLI que executa, na máquina do próprio usuário, os comandos que os agentes propõem e o pipeline de aprovação já aprovou — conectado ao engine por canal Phoenix autenticado por ticket de uso único.",
  "private": true,
  "type": "module",
  "bin": {
    "brabo-runner": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "node-pty": "^1.1.0",
    "phoenix": "^1.8.9"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/phoenix": "^1.6.7",
    "typescript": "^5.7.3",
    "vitest": "^4.1.10"
  }
}
