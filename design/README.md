# design/

Placeholder para o design system do Brabo (tokens, tipografia — Space
Grotesk/Archivo/IBM Plex Mono —, dark mode primário; ver `CLAUDE.md`).

Ainda não existe um design system implementado neste repositório. Quando
houver um (componentes React construídos, com `dist/` ou Storybook),
esta pasta passa a ser a fonte de verdade de fidelidade de UI referenciada
pelo `CLAUDE.md`, e pode ser sincronizada para o claude.ai/design via o
skill `design-sync`.
