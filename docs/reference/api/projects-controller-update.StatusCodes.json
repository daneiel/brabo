{"responses":{"200":{"description":"","content":{"application/json":{"schema":{"type":"object","properties":{"id":{"type":"string","example":"01JC4Z0000PROJETO0000000001"},"workspaceId":{"type":"string","example":"01JC4Z0000WORKSPACE00000001"},"name":{"type":"string","example":"Checkout"},"slug":{"type":"string","example":"checkout","description":"Unique within the workspace."},"workspaceDirName":{"type":"string","example":"checkout-3f2b1c8e","description":"Name of this project's physical workspace folder in PROJECT_WORKSPACES_ROOT (RN-109). `<slug>-<8 id chars>` on a new project; the raw UUID on a project created before this column existed. Frozen at creation — editing `slug` later doesn't recompute it."},"executionMode":{"type":"string","enum":["container","mounted","runner"],"example":"container","description":"WHERE the command executes (RN-169/RN-421 — ADR 0072/0104). `container`: the folder managed in PROJECT_WORKSPACES_ROOT. `mounted`: the user's folder in `workspacePath`, inside `BRABO_PROJECTS_BASE` and reached through the base bind-mount (ADR 0141). `runner`: the user's folder confirmed by the runner. Both `mounted` and `runner` report the confirmation in `workspaceVerifiedAt`."},"workspacePath":{"type":"object","example":null,"nullable":true,"description":"The user's folder absolute path — filled in for `mounted`/`runner`, always `null` in `container` mode."},"workspaceVerifiedAt":{"type":"object","example":null,"nullable":true,"description":"When the path was confirmed on disk. `null` = not yet verified. In `runner` the confirmation comes from the CLI connecting (RN-423); in `mounted` it is stamped when Infra starts the container and the folder is materialized (RN-501, ADR 0142). `container` never fills this field in. It records A confirmation, never a heartbeat."},"createdBy":{"type":"string","example":"01JC4Z0000USUARIO0000000001"},"taskBudgetMicros":{"type":"object","example":500000,"nullable":true,"description":"Token cap per dev agent task, in micro-USD. `null` uses the domain default."},"maxConsecutiveBlocked":{"type":"object","example":3,"nullable":true,"description":"Circuit breaker per dev agent (Phase 12b — RN-047): consecutive tasks ending blocked before stopping at idle_tripped. `null` uses the domain default."},"storyPromotion":{"type":"string","enum":["manual","auto"],"example":"manual","description":"Who promotes a story to `ready` (Phase 12c — RN-048). `manual`: the PO proposes and the user decides. `auto`: automatic promotion on creation (opt-in; where projects predating 12c ended up)."},"createdAt":{"type":"string","example":"2026-07-21T11:00:00.000Z","format":"date-time"},"updatedAt":{"type":"string","example":"2026-07-21T11:00:00.000Z","format":"date-time"}},"required":["id","workspaceId","name","slug","workspaceDirName","executionMode","workspacePath","workspaceVerifiedAt","createdBy","taskBudgetMicros","maxConsecutiveBlocked","storyPromotion","createdAt","updatedAt"],"title":"ProjectResponseDto"}}}},"400":{"description":"Invalid body. The `ValidationPipe` runs with `whitelist` and `forbidNonWhitelisted`, so an unknown field also fails."},"401":{"description":"No token, expired token, or invalid signature."},"403":{"description":"Insufficient role on the project."},"404":{"description":"Project doesn't exist or is invisible to the caller."},"409":{"description":"A project with this slug already exists in the workspace."},"429":{"description":"Rate limit per user or per IP."}}}