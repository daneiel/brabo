node_modules/
.pnpm-store/
dist/
dist-bin/
coverage/
.env
.env.local

# Elixir (apps/engine tem seu próprio .gitignore mais específico)
apps/engine/_build/
apps/engine/deps/

.DS_Store
.idea/
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
apps/web/types/

# Artefato do workflow de promoção (gerado por scripts/ci/promote.ts)
corpo-da-promocao.md

# Árvore que o docs-deploy.yml monta antes de empurrar para a gh-pages
# (ADR 0071). São ~2 mil arquivos gerados; um `git add -A` distraído os
# commitaria.
publicacao/

# Artefatos do E2E de navegador. O relatório HTML e os rastros/screenshots de
# falha são de UMA execução — o do CI vira artifact do job, não commit.
e2e/playwright-report/
e2e/test-results/
e2e/blob-report/
e2e/suporte/.estado-autenticado.json
